# 表紙画面＋評価方式(得点/ポイント)実装依頼

最終更新: 2026-09-18
依頼元: Cowork(要件整理担当)→ 実装: Claude Code CLI
前提: Phase 0(卓組みロジック本体)は`feature/phase0-table-logic`ブランチで実装済み・staging未マージ。本依頼は別ブランチとして、そのマージ前後どちらでも staging から新規に切って進めてよい(卓組みロジックとは独立した変更のため)。

---

## 0. まず読んでおくファイル

- `仕様書.md` 3章末尾(成績集計ルール)・7章(未確定事項) — 今回の依頼は7章の「点数算出ルールの詳細」を確定させるもの
- `基準.md` / `開発ロードマップ.md` — 現在地(Phase 0実装済み、Phase 1〜未着手)の確認
- `Phase0実装依頼.md` / `table_logic.py` — 前回の依頼の書き方・実装スタイル(UI/DB非依存の純粋関数＋pytest)を踏襲する

## 1. 背景・今回のゴール

ユーザーから「このアプリにまず表紙を作って、二つのボタンを表示。①得点で評価、②ポイントで評価、の二つに分けて」という依頼があった。ヒアリングで以下が判明している。

- **①得点で評価**: 素点＋ウマ＋オカ(＋飛び賞)を合計した**総合得点**で順位を決める方式
- **②ポイントで評価**: 各回戦(半荘)の順位に応じた**順位ポイント**の合計で順位を決める方式。②の合計ポイントが同点の場合、二次評価として①の総合得点で順位を決める
- 表紙はログイン後のトップ画面に置く(現在のメンバー管理画面などより前)
- 今回の依頼範囲: 表紙画面(UI)に加え、①②それぞれの**集計ロジック本体**(ウマ・オカ・飛び賞計算、順位ポイント計算)も仕様化・実装する

**追加のヒアリング結果(重要)**: ①の最初の持ち点・ウマ・オカ・飛び賞などの数値は大会ごとに違うため、大会主催者が任意に手入力できるようにする。②の順位ポイントも同様に大会ごとに違うため、手入力できるようにする。つまり「デフォルト値を持ったコード上のパラメータ」であるだけでなく、**実際に主催者が画面上で数値を入力できるUI**が必要。

**用語整理(重要)**: 仕様書.md 3章末尾には既に「蛇行方式の前回戦までの成績順は"合計ポイントの降順"」という記述があるが、これは今回の②(順位ポイント)とは別概念で、①の「累計得点」を指していたもの。混同を避けるため、以後は以下の用語に統一する。

- **累計得点**: ①方式で使う、素点＋ウマ＋オカの総和
- **累計順位ポイント**: ②方式で使う、各回戦の順位ポイントの総和

蛇行方式の卓組み(`table_logic.py`の`snake_rank`)に渡す「累計成績」は、大会の評価方式が①なら累計得点、②なら累計順位ポイントを使う(4.2手順2を評価方式に応じて一般化する形になる。table_logic.py自体の改修は不要で、呼び出し側が渡すcumulative_scoresを使い分けるだけでよい)。

## 2. パラメータ設計(すべて大会ごとに手入力できること)

以下はあくまで**画面の初期表示用デフォルト値**(未入力時のプレースホルダ・参考値)として使ってよいが、実際の値は大会作成・設定時に主催者が入力フォームで自由に変更できることが必須要件。

### 2.1 ①得点評価の入力項目

- 開始点数(持ち点)
- 返し点(オカの基準点)
- ウマ: 一人麻雀アプリ([[mahjong-solo-app]])で使っている「浮き人数連動テーブル」の形式(浮き人数0〜3人それぞれに4着分の順位点)を初期値の型として使ってよいが、数値はすべて入力可能にする
  - 初期値の参考(一人麻雀アプリの数値): 35,000点持ち／40,000点返し、1人浮き+48000/-8000/-16000/-24000、2人浮き+24000/+8000/-8000/-24000、3人浮き+12000/+8000/+4000/-24000、0人浮きはオカのみトップに加算
  - 同点の場合: 該当順位ぶんの順位点(オカ含む)を合計し、同点人数で折半する(この折半ロジックは固定でよい)
- オカ(0人浮き時にトップへ加算する額)
- 飛び賞: 半荘中に持ち点が0以下になった(トビが発生した)選手がいた場合に適用する額。金額0(飛び賞なし)も選べるようにする
  - **飛んだ人候補の判定**: 自動判定でよい。その半荘の素点入力欄で素点が0以下(0またはマイナス)の選手を「飛んだ人候補」としてUI上に表示する(チェックボックスを出す対象を絞り込むための目安)
  - **飛ばした人の指定**: 手入力(チェックボックス)。飛んだ人候補ごとに、同卓の他選手の中から「飛ばした人」を1人以上チェックできるようにする
  - **飛び賞の適用条件(重要)**: 飛び賞が実際に発生するかどうかは、素点の値そのものではなく**「飛ばした人」が1人以上チェックされているかどうか**だけで決まる。素点がちょうど0で、かつ誰もチェックされていない場合は「ちょうど0で(誰にも飛ばされず)終了した」という扱いとし、飛び賞は発生しない(減算も加算もなし)。素点がマイナスでも、チェックが無ければ同様に発生しない
  - **金額の配分**: 飛んだ人(チェックが1人以上ある選手)は入力された飛び賞額の全額を負担(総合得点から減算)。飛ばした人として複数人がチェックされた場合は、その飛び賞額をチェックされた人数で折半して各自の総合得点に加算する(端数が出る場合の丸め方は実装側の判断でよいが、合計が飛んだ人の負担額と一致すること)
  - 同一半荘で複数人が飛ぶこともあり得るため、飛んだ選手ごとに独立して上記の判定・配分を行う
- これらの数値(開始点数・返し点・ウマ表・オカ・飛び賞額)はすべて関数の引数／入力値として渡せるようにし、コード中に埋め込まない

### 2.2 ②ポイント評価の入力項目

- 順位ポイント表: 1位〜4位それぞれに何ポイントを与えるかを入力できるようにする(初期値の参考: 1位+3 / 2位+1 / 3位-1 / 4位-3)
- 大会を通した合計順位ポイントが同点の場合、2.1の①ロジックで計算した累計得点を二次評価に使い、それでも同点なら仕様書.md既存ルール(選手番号の若い順)で最終決定する

## 3. 実装対象

### 3.1 集計ロジック(純粋関数、新規モジュール例: `scoring_logic.py`)

`table_logic.py`と同じ方針(app.py・db.pyに依存しない、純粋関数、乱数は使わないため再現性の考慮は不要)で以下を実装する。

1. `compute_total_score(raw_scores, uma_config, tobi_amount=0, tobi_busters=None)` 相当の関数
   - 入力:
     - `raw_scores`: その回戦の素点(選手番号→素点の辞書)。飛んだ選手は素点が0以下(0またはマイナス)であることから自動判定する
     - `uma_config`: ウマ・オカ設定(開始点数・返し点・浮き人数別テーブル・オカ額。すべて呼び出し側から入力値として渡される)
     - `tobi_amount`: 飛び賞額(0なら飛び賞なし)
     - `tobi_busters`: 飛んだ選手ごとに「誰が飛ばしたか」を指定する辞書。例 `{飛んだ選手の選手番号: [飛ばした選手の選手番号, ...]}`。飛んだ選手がこの辞書にキーとして存在しない、またはリストが空の場合はその選手には飛び賞を適用しない
   - 出力: 各選手の「ウマ・オカ・飛び賞適用後の総合得点」
   - 浮き人数の判定・順位点テーブルの切替・同着時の順位点折半・飛び賞の減算(飛んだ選手から全額)＋加算(`tobi_busters`の人数で折半)ロジックを含む
   - 「誰が飛んだか」は素点から自動判定するが、「誰が飛ばしたか」は関数の入力として受け取るのみでよい(この情報をどの画面でどう入力させるかはPhase 4の範囲。今回は関数がこの構造のデータを正しく処理できることをテストで担保すれば十分)
2. `compute_rank_points(rankings, point_table)` 相当の関数
   - 入力: その回戦の順位(1位〜4位)、順位ポイント表
   - 出力: 各選手のその回戦の順位ポイント
3. `cumulative_score` / `cumulative_rank_points` の集計(大会を通した累計)は、既存の集計パターン(単純合計)でよいが、②の同点時に①の累計得点で二次評価する部分の比較関数を用意する(例: `rank_by_points_then_score(players, cumulative_points, cumulative_scores)` のような、②→①→選手番号の3段階タイブレークで順位配列を返す関数)

### 3.2 データモデル(仕様書.md 3.2 大会への追記)

- 大会(3.2)に「評価方式」フィールドを追加: `得点` / `ポイント`
- 評価方式が「得点」の大会は、ウマ・オカ・飛び賞設定(2.1の入力項目一式)を大会ごとに保持する
- 評価方式が「ポイント」の大会は、順位ポイント表(2.2の入力項目)を大会ごとに保持する
- このフィールド追加は大会管理(Phase 3、未着手)の実装時にDBスキーマへ反映すればよく、今回の時点では仕様書.mdへの追記とscoring_logic.py側のパラメータ設計が一致していれば十分(DBマイグレーションまでは今回のスコープ外)

### 3.3 表紙画面(UI、app.py)

- ログイン後のトップに新しい画面(表紙)を追加し、①「得点で評価」②「ポイントで評価」の2つのボタンを表示する
- ボタンを押すと、選んだ評価方式に応じた**数値入力フォーム**を表示する
  - ①を選んだ場合: 開始点数・返し点・ウマ表(浮き人数別4着分)・オカ・飛び賞額の入力欄(2.1参照、初期値は参考値をプレースホルダ表示)
  - ②を選んだ場合: 順位ポイント表(1〜4位)の入力欄(2.2参照)
- Phase 3(大会管理)・Phase 4(回戦・成績)がまだ無いため、入力値の保存先は今回は簡易でよい: 選んだ評価方式と入力されたパラメータ一式を`st.session_state`に保持し、既存のメンバー管理画面へ遷移する(画面上部などに「現在の評価方式: ◯◯」がわかる表示を出す程度)
- 大会作成・回戦実施の画面自体にこの入力フォームを接続する(大会ごとにDBへ保存する)のは、それぞれのフェーズ(Phase 3・4)の実装時でよい

## 4. テスト

`tests/test_scoring_logic.py`に以下を含むpytestを実装する。

- ①: 浮き人数0〜3人それぞれのテーブル適用、同着時の順位点折半、開始点数・返し点・テーブル・飛び賞額を差し替えても正しく動くこと(ハードコードされていないことの確認)
- ①(飛び賞): 素点が0以下(境界値の0点も含む)で飛んだ判定になること、飛んだ選手からの全額減算、飛ばした人が1人の場合・複数人の場合の折半加算、誰もチェックされなかった場合は適用されないこと、同一半荘で複数人が飛ぶケース
- ②: 順位ポイントの計算(入力された任意のポイント表で動くこと)、大会を通した累計、累計ポイント同点時に累計得点で二次評価されること、それも同点なら選手番号順になること
- 参考として`table_logic.py`のテストスタイル(TC-S/TC-O方式の入力・合格条件の書き方)を踏襲してよい

## 5. Definition of Done

- [ ] `scoring_logic.py`に①②の集計ロジックが純粋関数として実装済み(飛び賞含め、数値はすべてパラメータ化、ハードコードなし)
- [ ] `tests/test_scoring_logic.py`が実装され、pytest全件green(既存テストも壊さない)
- [ ] 表紙画面(2ボタン＋選択後の数値入力フォーム)がログイン後トップに実装され、選択した評価方式と入力値がセッションに保持される(表紙で入力するのは飛び賞の「額」までで、「誰が飛ばしたか」の入力UIはPhase 4で対応)
- [ ] `仕様書.md` 3.2(大会)・3章末尾・7章を、今回の決定内容(用語整理・評価方式フィールド・デフォルト数値)で更新
- [ ] `基準.md`・`開発ロードマップ.md`に今回の実装状況を反映

## 6. 今回の対象外

- 大会管理・回戦実施・成績入力画面そのもの(Phase 3・4、DBスキーマ変更含む)への実接続
- 表紙のデザイン作り込み(まずはボタン2つが機能すればよい)
