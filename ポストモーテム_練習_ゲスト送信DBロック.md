# ポストモーテム(練習): ゲストの成績送信中に DB がロックされる

作成: 2026-09-26(第53課題)
種別: **練習**(わざと起こした障害)。本番(web-production)・staging(web-staging-1395)には一切触れていない。
場所: 手元の Docker コンテナ `mahjong-app` の中だけ(練習用のデータ: 大会1件・16人・4卓)。

> ポストモーテムは「誰が悪いか」ではなく「仕組みのどこで止まったか・次にどう防ぐか」を書くもの。

---

## 1. 概要
半荘が終わって複数の卓が一斉に成績を送る場面を想定し、別のプロセスで DB を12秒間ロックしたまま、
ゲスト画面から卓1の成績を送信した。

- ゲストの画面が **英語のエラー(`sqlite3.OperationalError: database is locked` と Traceback)だけ** になり、
  入力欄も消えた。保存されたかどうかが画面から分からない
- 実際には **何も保存されていなかった**(送信は1つのトランザクションなので、途中で止まると全部なかったことになる)
- 主催者側には、ログに Streamlit の `Uncaught app execution` が1つ出るだけで、どの大会・何回戦・何卓かは分からなかった

## 2. 影響(本番で起きていたら)
- その卓の4人の成績が入らない。ゲストは「送れたのか」が分からず、主催者に口頭で確認する必要がある
- 入力欄ごと消えるため、ゲストは点数を覚えておいて入れ直す必要がある(ロックが解けて画面を再読み込みすれば、入力は残っている場合もある)
- 成績が壊れたり、二重に入ったりはしない(トランザクションと `UNIQUE(round_id, member_id)` 制約のため)

## 3. 時系列(練習・UTC)
| 時刻 | できごと |
|---|---|
| 06:53:51 | 変更前のコンテナで、DB を `BEGIN EXCLUSIVE` で12秒ロック → ゲストが送信 |
| 06:54:03 | 5.07秒後、画面が英語のエラーに。ログは `Uncaught app execution` のみ。保存0件 |
| 06:57 | 変更後(ブランチ `feature/guest-submit-logging`)のコンテナに入れ替えて、同じ操作 → **同じく英語のエラー** |
| 06:59 | `BEGIN IMMEDIATE`(ふつうの書き込みロック)でも同じ → 5.08秒後に英語のエラー |
| 06:59:42 | 送信の瞬間だけ失敗させる(練習用トリガー)→ **案内文が出て入力は残り、`guest_submit_failed` が1行出た** |
| 07:00:16 | トリガーを外して再送 → 保存され、`guest_submit_ok` が出た |

## 4. 原因
**直接の原因**: ロックが5秒以上続き、SQLite の待ち時間の上限(`sqlite3.connect` の既定値 5秒)を超えた。

**止まった場所(練習で初めて分かったこと)**: 送信の処理ではなく、**画面の一番上の `db.init_db()`**(`app.py` の `st.set_page_config` の直後)。
- `init_db()` はアプリの起動時だけでなく、**画面を再実行するたびに毎回** 呼ばれている
- その中の既存メンバーのバックフィル(`UPDATE members ... WHERE tenant_id IS NULL ...`)は、更新する行が0件でも **書き込みロックを取る**
- だから、ロックが長く続くと、どの画面でも送信より前の `init_db()` で止まる。ゲストの送信に付けた「保存されていません」の案内(今回の変更)は、このときは出番がない

## 5. 気づき方
| | 変更前 | 変更後(今回のブランチ) |
|---|---|---|
| 送信の瞬間に失敗 | 英語のエラー・ログに文脈なし | 案内文・入力は残る・`guest_submit_failed tournament=1 round=1 table=1 error=...` |
| 画面の先頭(`init_db`)で失敗 | 英語のエラー・`Uncaught app execution` | **変わらず**(今回の変更の範囲外) |

Railway では、Logs で `guest_submit_failed` と `Uncaught app execution` の両方を検索すれば気づける。

## 6. 実際の会でどのくらい起きそうか(計測)
コンテナの中で、練習DBのコピーを使い、Streamlit と同じく「画面の再実行ごとにスレッドで `init_db()` → 送信」を同時に走らせた。

| 条件 | 送信完了まで(中央値 / 最大) | locked |
|---|---|---|
| ロックの取り合いなし: `init_db` 1回 | 0.3ms / 2.2ms | – |
| ロックの取り合いなし: 1卓の送信 | 5.3ms / 6.8ms | – |
| 4卓が同時に送信 | 24ms / 49ms | 0 |
| 4卓が同時に送信 + ほかの再実行12件 | 149ms / 214ms | 0 |
| 8卓 + ほかの再実行24件 | 302ms / 440ms | 0 |
| 16卓 + ほかの再実行64件 | 708ms / 1,361ms | 0 |
| 32卓 + ほかの再実行200件 | 1,680ms / 2,314ms | 0 |

見立て:
- **数卓(4〜8卓)の一斉送信だけでは、5秒に届く可能性はとても低い。** 最大でも0.4秒ほどで、5秒まで10倍以上の余裕がある。32卓まで増やしても locked は0件だった
- 待ち時間の大部分は処理そのものではなく、SQLite がロックの空きを待つ「間隔をあけた再試行」。卓が増えるほど伸びるが、少しずつ伸びるだけで、急に跳ね上がることはなかった
- 5秒を超えるのは「同時に押した」ときではなく、**何かが書き込みロックを長く持ち続けた**とき。考えられるもの:
  - Railway のディスクが一時的に遅くなる(このPCのSSDより遅い可能性はあるが、**本番は計測していない**。本番には触らない約束のため)
  - 大きな処理(大会の削除で回戦・成績がまとめて消えるなど)や、Console から DB をコピー・バックアップしている最中
  - 何かの不具合で、トランザクションが閉じられずに残る
- つまり、起きる確率は低い。ただし **起きたときは全員の画面が同時に英語のエラーになり**、今回の案内文も出ない、という形になる

## 7. 再発防止・改善(案。どれもまだやっていない)
| 案 | 効果 | 大きさ |
|---|---|---|
| ① `init_db()` を画面の再実行ごとではなく、起動時に1回だけにする(`st.cache_resource` など)(10/2 の後に実施) | 画面を表示するだけでは書き込みロックを取らなくなる。ロックがあっても閲覧はでき、止まるのは実際に書く瞬間だけ → 今回の案内文が働く | 小(数行)。10/2 の後 |
| ② 画面の先頭で `database is locked` を拾い、「混み合っています。数秒後に再読み込みしてください」を出す | ①ができるまでのつなぎ | 小 |
| ③ SQLite を WAL モードにする | 書き込み中でも読める | 中(Railway のボリュームで要確認) |
| ④ 10/2 当日の運用: (1) 会の最中はデプロイしない (2) Console から DB に触らない (3) 大会の削除など大きな操作をしない (4) Logs で `guest_submit_failed` と `Uncaught app execution` を時々検索する | ロックが長く続く原因を減らし、起きたら気づく | 運用だけ |

## 8. うまくいったこと
- 送信は1つのトランザクションなので、失敗しても「半分だけ保存」にならなかった
- コンテナを止めて `docker start` すると、約1.6秒で応答が戻り、ボリュームの成績は残っていた
- 送信の瞬間の失敗は、今回の変更で「入力を残したまま案内する・ログで場所が分かる」ようになった

## 付録: 練習2(アプリが止まる)
- `docker stop` 中: 開いていたゲスト画面は、上に「CONNECTING」と出て、前の内容が表示されたまま。新しく開こうとすると接続できない
- `docker start` から約1.6秒で `/_stcore/health` が ok。保存済みの成績(4件)は残っていた
