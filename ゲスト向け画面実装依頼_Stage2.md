# ゲスト向け画面 実装依頼(Stage 2)

作成: 2026-09-18(Cowork)
対象リポジトリ: worldmahjongunion-sys/mahjong-table-assignment
前提: PR #28(Stage 1:大会管理・回戦実施・ゲスト共有リンク発行)が staging にマージ済みであること

---

## 1. 目的

大会の参加者(ゲスト)が、主催者から LINE グループで共有されたトークン付き URL(`?guest=<トークン>`)を開くだけで、ログインやアカウント作成なしに次の3つを行えるようにする。

1. 卓組み結果の閲覧
2. 成績(素点)の入力 — 卓ごとに代表者1人が4人分をまとめて入力
3. 順位・累計成績の閲覧

想定利用場面は火曜日の麻雀道場・サークル会・ワンデー大会。参加者の多くは LINE 内蔵ブラウザ(WebView)でスマホから開く。

---

## 2. 作業の進め方

- ブランチ: PR #28 マージ後の最新 staging から `feature/guest-screens` を切る
- 完了したら push して staging への PR を作成する。PR 本文に pytest の実行結果(件数と green/red)を明記する
- **マージはしない**(ユーザーの指示待ち)
- 主催者向け画面(メンバー管理・評価方式設定・大会作成・卓組み実行)のログイン必須はこれまでどおり変更しない
- 既存の `db.get_tournament_by_guest_token`(Stage 1 で実装済み)を使って大会を特定する。トークンの保存方式(sha256 ハッシュのみ保存、生トークン非保存)は変更しない

---

## 3. 決定済みの仕様

### 3.1 ゲスト画面への入口

- `?guest=<トークン>` 付きでアクセスした場合、ログイン画面を出さずにゲスト専用画面を表示する
- トークンが無効(存在しない・再発行で失効済み)の場合は、「このリンクは使えません。主催者に最新のリンクを確認してください」と表示するだけにする。主催者画面やログイン画面へは誘導しない
- ゲスト画面からは主催者向けの画面・他の大会のデータに一切アクセスできないこと

### 3.2 卓組み結果の閲覧

- 最新の回戦の卓組みを卓番号順に表示する(卓番号、東家・南家・西家・北家の選手名)
- 抜け番がいる場合は「抜け番: ○○さん」と表示する
- 過去の回戦の卓組みも切り替えて見られるようにする
- 各卓に「入力済み/未入力」の状態を表示する

### 3.3 成績(素点)の入力

- 入力者は卓ごとに代表者1人。回戦と卓を選び、その卓の4人分の素点をまとめて入力する
- **送信した時点で確定**する。主催者の承認は挟まない
- **4人の素点合計が「開始点数×4」に一致しない場合は送信できない**。どれだけずれているかを赤字でやさしく表示する(例:「合計が 99,000 点です。100,000 点になるよう確認してください」)
- マイナスの素点(飛び)を入力できること
- 評価方式が「得点」の大会では、既存の飛び賞ルールどおり「飛ばした人」を選手ごとのチェックボックスで入力できるようにする(複数可。誰もチェックしなければ飛び賞なし)。評価方式が「ポイント」の大会ではこの欄は出さない
- 送信前に確認画面(または確認表示)を出し、「送信すると修正できません」と明示する
- **送信後はゲスト側から修正できない**。入力済みの卓を選んだ場合は、確定済みの素点を表示し「修正は主催者に依頼してください」と案内する
- 同じ卓に2人がほぼ同時に送信した場合、先に届いたものだけを確定し、後のものは「すでに入力済みです」と表示して保存しない(DB 側の制約で担保する)
- 誰がいつ入力したかを後から追えるよう、成績レコードに入力日時と入力経路(ゲスト/主催者)を残す

### 3.4 順位・累計成績の閲覧

- 大会の評価方式に応じた現在の順位表を表示する(既存の `compute_standings` を使う)
  - 得点方式: 総合得点(素点＋ウマ＋オカ＋飛び賞)の降順
  - ポイント方式: 累計順位ポイント → 素点合計 → 選手番号の若い順
- 表示項目は順位・選手名・合計(ポイントまたは得点)・参加回戦数程度でよい

### 3.5 主催者側の修正機能

- 主催者画面(ログイン必須)から、確定済みの素点を修正できるようにする。Stage 1 の画面で既に修正できる場合はそれを使い、新たに作らなくてよい
- 修正時も「合計が開始点数×4」のチェックを同様にかける
- 修正したら入力経路を「主催者」、日時を修正日時に更新する

### 3.6 ゲストリンクの再発行

- 主催者画面に「リンクを再発行」ボタンを置く。押すと新しいトークンを発行し、**古いトークンは即座に使えなくなる**
- 再発行前に「古いリンクは使えなくなります。LINE に新しいリンクを貼り直してください」と確認を出す
- Stage 1 で既に同等の機能がある場合は、その動作(古いトークンの失効)がテストで担保されていることを確認するだけでよい

### 3.7 スマホ表示

- LINE 内蔵ブラウザでの利用が中心なので、スマホ幅(375px 前後)で横スクロールせずに使えること
- 素点入力欄はスマホの数字キーボードで入力しやすくし、特にマイナス記号が入力できることを確認する
- ボタンは指で押しやすい大きさにする

---

## 4. 実装前に確認して報告してほしいこと

以下は推測で進めず、実装前にコードを確認して結果を報告すること。判断が必要なものは報告後にユーザーの指示を待つ。

1. **ポイント方式の大会が「開始点数」を持っているか。** 持っていない場合、3.3 の合計チェックができない。その場合は対応案(例:ポイント方式にも開始点数の入力欄だけ足す)を示して指示を待つ
2. 素点の単位(例: 25000 と入力するのか 250 と入力するのか)が Stage 1 でどう決まっているか。ゲスト画面もそれに合わせる
3. Stage 1 の主催者画面で、確定済み素点の修正とリンク再発行がどこまでできているか

---

## 5. テストで担保してほしいこと

- 有効なトークンでゲスト画面が開ける/無効・失効済みトークンでは案内文だけが出る
- ゲスト画面から他の大会・主催者向けデータにアクセスできない
- 合計が開始点数×4 と一致しない入力は保存されない(境界値:1点ずれ、ぴったり)
- マイナスの素点を含む入力が正しく保存される
- 送信済みの卓に再送信しても上書きされない(同時送信を含む)
- 主催者による修正で値と入力経路・日時が更新される
- リンク再発行後、古いトークンでアクセスできない
- 得点方式では飛ばした人の入力が飛び賞に反映され、ポイント方式では入力欄が出ない
- 既存テスト(現在 188 件)がすべて green のまま

---

## 6. 完了条件(DoD)

- 3章の仕様がすべて実装され、5章のテストが green
- 仕様書.md・開発ロードマップ.md・基準.md を実装内容に合わせて更新(ロードマップの「ゲスト向け画面(Stage 2)」行を完了に)
- `feature/guest-screens` を push し、staging への PR を作成(本文に pytest 結果と、依頼文書と異なる判断をした箇所があればその理由を記載)
- マージはしない

---

## 7. 今回のスコープ外

- アカウント作成必須への移行(有料化のめどが立った段階で別途)
- 大会をまたいだ通算成績
- ゲストによる卓組みの実行や変更
