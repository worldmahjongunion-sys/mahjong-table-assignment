# 開発〜運用の型

mahjong-table-assignment の開発・運用を通して確立した、再利用可能な基本パターン。

## 1. ブランチ戦略

- `main`：本番環境（production）に対応する、常に安定した状態を保つブランチ
- `staging`：検証環境（staging）に対応する開発用ブランチ。まずここで変更を試す
- 変更は必ず staging → 検証 → main への昇格PR、の順で進める
- main への直接pushはしない

## 2. CI/CD（自動デプロイ）

- Railway が GitHub と連携しており、各ブランチへのpush（マージ）で対応する環境に自動デプロイされる
- デプロイ前チェックとして、Procfile と各環境の「Custom Start Command」が一致しているか必ず確認する（両者が食い違うと、デプロイは成功してもコードが反映されない）

## 3. 環境分離

- staging と production は別々のRailwayサービス・別々のデータベースを持つ
- 本番相当の変更（特にDBスキーマ変更）は、必ずstagingで先に検証してからmainに昇格する

## 4. データを壊さない運用

- 本番DBへの変更前には `安全チェックリスト.md` の手順に従いバックアップを取得する
- バックアップ・復元は Railway の Console（ファイルブラウザ）経由で `/data/mahjong.db` を出し入れする

## 5. 障害対応

- `障害復旧の型.md` の「気づく→切り分け→ロールバック→復旧確認」に従う
- 気づく手段として UptimeRobot による外部監視（5分間隔・ダウン時メール通知）を常設する

## 6. 独自ドメイン・SSL

- ドメインは1つ取得すれば、サブドメイン（例：app.xxx.com）は追加費用なしで複数アプリに使い回せる
- Railway の Custom Domain 機能で CNAME/TXT レコードを発行し、レジストラ（Namecheap等）のDNSに登録する。SSL証明書はRailwayが自動発行する

## 一番効いた回

第14回（データを壊さず育てる：本番DBの運用）が一番効いた。
理由：Procfileを修正しても、Railway側の「Custom Start Command」という別設定が優先されてしまい、デプロイ自体は成功するのにマイグレーションが本番に反映されないという落とし穴を実際に踏んだ。デプロイの成功＝正しく反映された、ではないことを痛感し、以降は「デプロイ成功」と「意図通りに動いているかの確認」を別工程として必ず分けるようになった。この教訓は、今回のRound 15のカスタムドメイン設定でも「設定した＝反映された、ではない」という姿勢で臨むことにつながった。
