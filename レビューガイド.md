# レビューガイド

最終更新: 2026-08-26

PRをレビューする（あるいは自分の変更をPRとしてセルフレビューする）際に見る観点と、コメントの書き方、そして「なぜmain保護＋必須レビューが必要か」をまとめたガイド。

---

## 1. レビューで見る観点

| 観点 | 確認内容 | このSaaSでの具体例 |
|---|---|---|
| **目的整合性** | PRのタイトル・説明で述べている目的と、実際の変更が一致しているか | 「READMEにstaging環境の記載を追記する」PRのはずが、`app.py`のロジックまで変わっていないか |
| **影響範囲** | 変更がどこまで波及するか。想定外の副作用がないか | `db.py`の変更なら、Railway Volume上の既存SQLiteデータのスキーマや、`migrate_to_auth.py`のような移行スクリプトとの整合性が崩れないか |
| **可読性** | 命名・関数の長さ・構造が理解しやすいか | `app.py`（Streamlit画面）に`db.py`（DB操作）のロジックが混入していないか。画面とデータアクセスの分離が保たれているか |
| **テスト・動作確認** | 実際に動かして確認したか | 卓組みロジック（Phase 0）なら`Phase0テストケース.md`（蛇行15件・ワンデー14件）に沿って確認したか。画面変更なら`streamlit run app.py`でローカル起動して壊れていないか確認したか |
| **コミットの粒度とメッセージ規約準拠** | 1コミット1目的になっているか、`コミットメッセージ規約.md`のtype/scopeに沿っているか | `9c887eb`のような「メンバー管理＋ログイン＋Railway設定＋ドキュメント」を1コミットに詰め込む形になっていないか |

---

## 2. 建設的コメントの型

**基本の型**: 「良い点を先に、具体的に伝える」→「改善点は断定ではなく提案形で伝える」→「必要なら理由や代替案を添える」。

### NG例 → OK例

| NG例 | OK例 |
|---|---|
| このコミット分割が雑。 | READMEの追記自体は良い変更です。ただ`docs`と`chore`の変更が1コミットに混ざっているので、`コミットメッセージ規約.md`のtypeに沿って分けると、後から見返しやすくなりそうです。 |
| 変数名が意味不明。 | ロジックの実装、テストケースまで揃っていて良いですね。1点だけ、`tmp`という変数名がやや分かりにくいので、`remaining_players`のような名前にすると読みやすくなるかもしれません。 |
| このエラーハンドリング要らなくない？ | 例外処理を丁寧に入れてくれていて安心感があります。ここは呼び出し元で既にバリデーション済みの値しか渡らない箇所なので、この`try/except`は削ってシンプルにしても良いかもしれません、どう思いますか？ |
| なんでこの設計にしたの？ | この実装で動作は問題なさそうです。設計の背景（なぜこのアプローチを選んだか）をPRの説明かコメントに一言残しておくと、後で見返す人（未来の自分含む）が助かりそうです。 |

**ポイント**: 「〜すべき」「〜が間違っている」のような断定・否定形ではなく、「〜するとより良さそう」「〜はどうでしょう」のような提案形にすると、相手（自分自身のセルフレビューでも同じ）が受け取りやすい。

---

## 3. なぜmain保護＋必須レビューが必要か

| 理由 | 説明 | このSaaSでの具体例 |
|---|---|---|
| **単独ミス防止** | 自分では気づきにくい見落とし（タイポ、条件分岐の抜け漏れ）を、もう一度目を通すことで発見できる | 「ユーザー名を小文字に正規化」した不具合修正（`7ef0d1f`）のような、ログイン周りの見落としは特にレビューで早期発見しやすい種類のバグ |
| **知識分散** | 変更の背景・意図がPRとレビューコメントに記録として残り、実装者一人に知識が閉じない | 将来Phase 0（卓組みロジック）を実装する際、なぜ今の仕様（`仕様書.md`）に落ち着いたかがPRの議論として追える |
| **変更履歴の説明責任** | 「誰が・何を目的に・いつ変更したか」をPR単位で説明できる記録が残る。`git log`だけでは「なぜ」が分かりにくい | PR #7〜#9のように、ドキュメント追加の意図をPR説明に残しているので、後から見返しても経緯が分かる |
| **CIとの二重防御** | 自動テスト/Lintは拾えない「設計判断の妥当性」「仕様との整合性」を人のレビューが補い、機械的チェックと人的チェックの二重の防御線になる | このリポジトリはまだCIを導入していないため、現状はレビューが唯一の防御線。CIを導入した後も、CIが通ることとレビューで妥当性を確認することは別の観点であり続ける |
| **1人開発でも効く理由** | 「PRの形にまとめる」「一旦間を置いてから見返す」という行為自体が、自分の変更を客観視するセルフレビュー効果を持つ | 実際、このSaaSでも初期に`9c887eb`のような巨大一括コミットが発生している。PR単位でセルフレビューする習慣があれば、コミットを分割する判断がその場で入りやすかった可能性が高い |

---

## 補足: このリポジトリでの制約

執筆時点（2026年8月）で、GitHubのブランチ保護ルール（classic branch protection rules）は、**プライベートリポジトリ×個人のFreeアカウント**の組み合わせではそもそも設定できない（GitHub Pro/Team/Enterprise相当のプランが必要）。

このリポジトリ（`worldmahjongunion-sys/mahjong-table-assignment`）もプライベート・個人Freeアカウントで運用しているため、「mainへの直接push禁止」「レビュー必須」はGitHubの機能として強制されているわけではない。つまり本ガイドの内容は、ツールに強制させるルールではなく、**開発者自身が守る運用ルール**として位置づけている。
