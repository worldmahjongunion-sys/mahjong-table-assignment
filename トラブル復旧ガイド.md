# トラブル復旧ガイド

最終更新: 2026-08-26

Git×Railway応用編（第10回）で実際に手を動かして確認した、「やらかし」からの復旧手順と、Railway本番のロールバック手順。

---

## 1. やらかし→復旧の記録（実演ログ）

すべてローカルの使い捨てブランチ（`practice/git-recovery-lesson10` など）で実施。本番・`main`には一切影響なし。実演後、練習用ブランチはすべて削除済み（GitHubにもpushしていないため痕跡なし）。

### シナリオA: `revert` — 間違ったコミットを、履歴を消さずに打ち消す

```bash
# 正常なコミット
git commit -am "feat: 練習用メモファイルを追加"

# わざと間違えたコミット（例: デバッグ用キーを誤ってコミット）
git commit -am "fix: デバッグ用キーを追加（わざと間違えたコミット）"

# revertで打ち消す（元のコミットは消さず、打ち消す新しいコミットを作る）
git revert <間違えたコミットのSHA> --no-edit
```

**結果**: ファイル内容は元に戻ったが、履歴には「間違えた事実」も「直した事実」も両方残った。pushして共有済みのブランチ（main等）で間違いを直すときはこれが安全。

### シナリオB: 「消しちゃった」を `reflog` で復元する

```bash
# 事故発生: 2つ前のコミットまでうっかり戻してしまう
git reset --hard HEAD~2
# → git log からコミットが消えたように見える

# reflogでHEADの移動履歴を確認（消えたはずのコミットが見える）
git reflog
# 例: b7dfb1c HEAD@{1}: revert: Revert "fix: デバッグ用キーを追加..."

# 事故る直前の状態に戻す
git reset --hard HEAD@{1}
```

**結果**: 完全復元できた。`reflog`はローカルの`HEAD`が動いた記録なので、`reset --hard`で見失っても消えたわけではなく、ポインタが移動しただけ。※`reflog`はローカル限定・デフォルト90日で消える。

### シナリオC: ブランチを間違えて削除 → `reflog`で復元

```bash
# 未マージのブランチを作業
git checkout -b practice/wip-feature
git commit -am "feat: 実験的な機能を試作（未マージ）"
git checkout practice/git-recovery-lesson10

# 事故発生: マージしていないブランチをうっかり強制削除
git branch -D practice/wip-feature
# → Deleted branch practice/wip-feature (was 5f04c4c).  ← このSHAが手がかり

# 削除されたコミットのSHAから、ブランチを復元
git checkout -b practice/wip-feature-recovered 5f04c4c
```

**結果**: ブランチごと復元できた。SHAさえ分かれば（`git branch -D`の出力、または`git reflog`で見つけられる）、削除したブランチはいつでも`git checkout -b <新しい名前> <SHA>`で復活できる。

### シナリオD: `stash` — コミットせずに一時退避

```bash
# 作業中（コミットしたくない途中の変更）がある状態で、緊急対応に切り替えたい
git stash push -m "練習: 卓組みメモの途中作業"
git stash list
# → 別の作業（緊急のバグ修正ブランチなど）をする

# 作業に戻ってきたら復元
git stash pop
```

**結果**: 未コミットの変更を「棚上げ」して作業ディレクトリをきれいにし、後で`pop`で戻せることを確認。ブランチを切り替える必要すらなく使える。

---

## 2. revert / reset / reflog / stash の使い分け

| コマンド | どんな時に使うか | このSaaSでの例 | 危険度 |
|---|---|---|---|
| **revert** | pushして共有済みのコミット（特にmain）の間違いを、履歴を保ったまま打ち消したい時 | mainにマージしたPRの内容が本番で問題を起こした → `git revert <マージコミット> -m 1` して再度push → Railwayが自動で前の状態に再デプロイ | 低（安全・共有ブランチOK） |
| **reset** | まだpushしていないローカルのコミットを、なかったことにしたい/積み直したい時 | 作業ブランチでpushする前に「typoコミットが3つに分かれて汚い」→ `git reset --soft HEAD~3`してから1つにまとめ直す | 中〜高（push済み・共有ブランチでは危険。`--hard`は作業内容も消す） |
| **reflog** | resetやbranch削除で「消えた」ように見えるコミットを探し出したい時 | `git reset --hard`で作業ブランチの未pushコミットを丸ごと吹き飛ばした → `git reflog`でSHAを探して`git reset --hard HEAD@{N}` | 復元用なので安全（ただしローカル限定・約90日で消える） |
| **stash** | コミットするほどまとまっていない作業途中の変更を、一旦どかして別の作業（緊急のブランチ切り替えなど）をしたい時 | Phase 0ロジック実装中に、本番の緊急バグ報告が来た → `git stash`で退避 → `fix/`ブランチで緊急対応 → 終わったら`git stash pop`で作業に復帰 | 低（いつでも戻せる。ただし溜め込んで何の変更か忘れると事故る） |

**判断の目安**: 「もうpushして人（またはRailway）が見た/使った」→ **revert**。「自分のローカルだけで、まだ誰も見ていない」→ **reset**。「何か消えた/見失った」→ **reflog**。「今は確定させたくないが消したくもない」→ **stash**。

---

## 3. 本番(Railway)のロールバック手順

悪いデプロイをしてしまった時の、2つのやり方。状況に応じて使い分ける。

### 方法1: Railwayダッシュボードから即座に戻す（最速・応急処置向け）

1. Railwayのプロジェクトを開き、対象サービスの **Deployments** タブを開く
2. デプロイ履歴が新しい順に並んでいるので、問題が起きる前の、正常に動いていたデプロイを探す
3. そのデプロイの「⋮」メニューから **Redeploy**（バージョンによっては **Rollback**）を選択
4. 直前の正常なビルド成果物がそのまま再デプロイされる（GitHubへのコミットやpushは不要）

**メリット**: 数十秒〜数分でユーザー影響を止められる。**注意点**: Gitの`main`の中身とRailwayの実際の稼働内容が一時的にズレる（「本番はロールバックしたが、mainには悪いコミットが残ったまま」という状態になる）ので、応急処置後は必ず方法2でGit側も揃える。

### 方法2: Gitで`revert`してpush（正攻法・履歴に残る）

```bash
git checkout main
git pull origin main
git log --oneline -5          # 問題のコミット/マージコミットのSHAを特定
git revert <問題のコミットSHA> -m 1 --no-edit   # マージコミットの場合は -m 1 が必要
git push origin main
```

`main`へのpushをRailwayが検知して、打ち消した（＝問題発生前と同じ内容の）状態を自動で再デプロイする。上記1のシナリオAで体験した`revert`が、まさにこの本番ロールバックの土台になる。

**使い分け**: 影響が大きく一刻を争う（ユーザーがエラー画面を見ている等）→ まず方法1でダッシュボードから即座に止血 → 落ち着いたら方法2でmainの履歴も正しい状態に揃える。軽微な問題で急いでいなければ、最初から方法2だけで十分。
