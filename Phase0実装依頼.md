# Phase 0 実装依頼 — 卓組みロジック本体

最終更新: 2026-09-18
依頼元: Cowork（要件整理担当）→ 実装: Claude Code CLI

---

## 0. まず読んでおくファイル(このリポジトリ内、正本)

実装前に必ず以下を読むこと。本書はこれらの要約・タスク化であり、矛盾があれば以下が正。

- `仕様書.md` 4章「卓組みロジック仕様」— アルゴリズムの正本
- `Phase0テストケース.md` — 合格条件の正本(蛇行15件・ワンデー14件、計29件)
- `基準.md` / `開発ロードマップ.md` — 現在地の確認(Phase 2まで完了・Phase 0が次の最優先)
- `開発運用の型.md` / `コミットメッセージ規約.md` — ブランチ運用・コミット規約

## 1. 今回のゴール

このアプリの中核価値である「卓組みロジック」本体(蛇行方式・ワンデー4半荘方式)を、**Streamlit/DBから独立した純粋関数**として実装し、`Phase0テストケース.md` の全29ケースに対応するpytestテストをgreenにする。

UI組み込み(Phase 1)・DB接続(Phase 3・4)は今回の対象外。`app.py` には現在「卓組み生成機能は未実装」というコメントがあるが、これは変更しない(UI接続は別フェーズ)。

## 2. 実装対象

### 2.1 モジュール構成(提案。実装者の判断で調整可)

- 新規モジュール(例: `table_logic.py`)に、以下の純粋関数を実装する
  - 入力: メンバーの選手番号リスト、(蛇行方式のみ)大会内累計成績または出場回戦数履歴、乱数シード
  - 出力: 卓ごとのメンバー割当+スタート位置(東南西北)、抜け番リスト
  - `app.py`・`db.py` に依存しない(importしない)こと。DBアクセスやStreamlit APIを呼ばないこと
- テストは `tests/test_table_logic.py`(モジュール名に合わせて調整)に実装

### 2.2 実装するロジック(仕様書.md 4章に対応)

1. **抜け番ルール(4.1、共通)**
   - 卓数 T = floor(N/4)、抜け番人数 R = N mod 4
   - 抜け番はその大会での出場回戦数が最多の人から選ぶ(途中参加者が優先的に抜け番になるのを防ぐ)
   - 出場回戦数が同数の場合、直前回戦の抜け番者を除外してランダム選出(除外すると候補がR人未満になる場合は除外しない)
2. **蛇行(スネーク)方式(4.2)**
   - 第1回戦はランダムな仮順位(過去大会の通算成績は使わない)
   - 第2回戦以降は大会内累計成績(合計ポイント降順、同点は選手番号昇順)
   - T人ずつのブロックに分割し、ブロックごとに割当方向を反転(ボウストロフェドン)
   - 卓内着席は卓組みに使った順位の上位から「東→南→西→北」
3. **ワンデー4半荘方式(4.3)**
   - 席順一巡(東南西北を実際に出場した回戦の範囲内で1回ずつ)が必達制約・最優先
   - 同卓重複の最小化は努力目標(席順一巡と衝突する場合は席順一巡を優先)
   - 事前定義の組合せ表は持たず、汎用アルゴリズム(半荘1はランダム、半荘2以降は未経験位置の組合せを優先探索→貪欲法で重複最小化)で生成する
   - 重複数が同じ候補が複数ある場合、選手番号が小さい人がより若い卓番号に入る候補を一意に採用する

### 2.3 設計上の必須条件(全体共通)

- 選手番号は連番・1始まりを前提にしない(くじ引き採番に対応するため、配列インデックスではなく値として扱う)
- 乱数を使う箇所(抜け番の同数タイ選出、蛇行方式の第1回戦仮順位、ワンデー方式の半荘1割当・貪欲法候補生成)はすべて外部からシード注入可能にする。同一シード・同一入力から同一結果を再現できること
- 途中参加・途中離脱(出場回戦数0の新規参加者、離脱者の除外)に対応できる関数インターフェースにする

## 3. テスト

- `Phase0テストケース.md` の29ケース(TC-S-01〜15、TC-O-01〜14)を pytest で実装する
- 蛇行方式は結果が一意に決まる(決定的)ため、期待値を具体的な番号で検証してよい
- ワンデー4半荘方式はヒューリスティックなため、期待値は不変条件(席順一巡の成立・重複数が理論下限以上であること等)として検証する
- 既存テスト(`tests/` 配下)を壊さないこと。`pytest` 全件green が完了条件

## 4. 進め方(開発運用の型.md準拠)

- `staging` から切ったフィーチャーブランチで作業する(`main` への直接pushはしない)
- コミットメッセージは `コミットメッセージ規約.md` に従う
- 完了後: フィーチャーブランチ→`staging`へPR→検証→`main`への昇格PR、の順
- 今回はDBスキーマ変更を伴わないロジック単体実装のため、`安全チェックリスト.md` のバックアップ手順は必須ではないが、CI(`.github/workflows/tests.yml`)が通ることは必須

## 5. Definition of Done

- [ ] 抜け番・蛇行方式・ワンデー4半荘方式が純粋関数として実装済み(UI/DB非依存)
- [ ] `Phase0テストケース.md` 全29ケース相当のpytestが実装され、全件green
- [ ] 既存テストも含めpytest全体がgreen、CIが通る
- [ ] `基準.md`・`開発ロードマップ.md` のPhase 0該当箇所を「実装済み」に更新

## 6. 今回の対象外

- StreamlitのUIへの組み込み(Phase 1)
- 大会・回戦・成績のDB接続(Phase 3・4)
- 点数算出ルール(ウマ・オカ等、仕様書7章「未確定事項」)
