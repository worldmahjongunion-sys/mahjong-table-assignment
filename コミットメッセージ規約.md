# コミットメッセージ規約

最終更新: 2026-08-26

過去のコミット履歴を振り返り、読みにくかった点を洗い出したうえで定めた、今後のコミットメッセージの型。

---

## 1. 過去の指摘事項

| コミット | メッセージ | 問題点 |
|---|---|---|
| `9c887eb` | 麻雀卓組みアプリ: メンバー管理とログイン機能、Railway公開設定 | 703行・11ファイルを1コミットに詰め込んだ「初期一括コミット」。メンバー管理・ログイン・Railway設定・ドキュメント3本が全部混ざっていて、これ1個で何が変わったかをレビュー・revertできない |
| `aa1c4f3` | ドキュメントを現状の実装に合わせて更新（提出用） | 「現状の実装に合わせて」は差分を見ないと中身が分からない。3つのドキュメントの何を直したのか書かれていない |
| `1475ff1` | Clarify purpose of the application in README | 他が日本語なのに突然英語で言語が不統一。「Clarify」だけでは具体的にREADMEのどの記述をどう変えたか不明。README1行の変更のためだけに別ブランチ(edit-b)を切っている |
| `96db501` | Clarify automation of table arrangement process | 同上。README1行変更でブランチ(edit-a)を切っている |
| `3380d23` | Merge branch 'main' into edit-b | 内容を持たない「配線」コミット。作業ブランチの中でmainを取り込んだだけで、履歴を読むときのノイズになる |

一方、`40fb93c`（runtime.txtを削除〈Railwayのビルドで失敗するため〉）と `7ef0d1f`（ユーザー名を小文字に正規化〈…不具合の修正〉）は「何を・なぜ」が一行で分かる良い例だった。

---

## 2. コミットメッセージの型

Conventional Commitsをこのプロジェクト向け（日本語・ドキュメント多め）に簡略化した型を採用する。

```
<type>(<scope>): <変更内容を一言で（体言止め）>

<本文（省略可）：なぜ変更したか、影響範囲など>
```

### type一覧

| type | 用途 |
|---|---|
| `feat` | 新機能追加 |
| `fix` | 不具合修正 |
| `docs` | ドキュメントのみの変更 |
| `refactor` | 挙動を変えないコード整理 |
| `chore` | ビルド・デプロイ設定、依存関係など |

**scope例**: `member`（メンバー管理） / `auth`（ログイン） / `deploy`（Railway） / 省略可

### ルール

- 1コミット = 1目的。複数の意味が混ざるなら分割する
- 概要は日本語で統一し、体言止め（「〜を追加」「〜を修正」）で書く
- 「なぜ」はdiffから読み取れないので、必要なら本文に書く

### 書き直し例

```
# 9c887eb（本来はこう分割すべきだった）
feat(member): メンバー登録・一覧機能を追加
feat(auth): 招待コード制のログイン機能を追加
chore(deploy): Railway公開設定を追加（Procfile, requirements.txt）
docs: 仕様書・基準・開発ロードマップを追加

# aa1c4f3
docs: 仕様書・基準・ロードマップを実装済み機能に合わせて更新

Phase2実装（メンバー管理・ログイン）に伴い、未実装扱いだった記述を
実装済みに更新。提出用に内容を最新化。

# 1475ff1
docs: READMEにアプリの目的（大会運営の負担軽減）を明記

# 96db501
docs: READMEに卓組み自動化の説明を補足

# 参考: 既に良い例を型に当てはめると
chore(deploy): runtime.txtを削除してRailwayのビルド失敗を解消
fix(auth): ユーザー名を小文字に正規化してログインエラーを修正
```

---

## 3. マージ vs リベース

**merge**: 2本の履歴を統合する「マージコミット」を新しく作る。元のコミットはそのまま残り、履歴は分岐→合流のグラフになる。非破壊的（既存コミットを書き換えない）。

**rebase**: あるブランチのコミット群を、別のコミットの上に「乗せ替える」。コミットは新しいSHAとして作り直される。結果、履歴が一直線(linear)になる。破壊的（元のコミットを書き換える＝pushして他人と共有済みのブランチでやると事故る）。

### このSaaSの実例で

- **PR #1〜#7をmainに取り込む場面**（`Merge pull request #N`）→ **merge が正解**。GitHub上で複数人（将来のレビュワー含む）が見る`main`の履歴なので、「どのPRがいつ取り込まれたか」の記録を残す必要がある。実際そう運用できている。
- **`3380d23` "Merge branch 'main' into edit-b"** → これは**rebaseにすべきだった**ケース。`edit-b`という自分だけの作業ブランチの中で最新mainを取り込みたいだけなら、`git rebase main` すれば edit-b のコミットが最新mainの上に乗り、この「配線」マージコミットは発生しない。
- **`edit-a`/`edit-b`のようなREADME1行修正** → そもそもブランチを切るほどの規模ではないが、切るなら rebase して main に fast-forward させれば、"Merge pull request #N" というマージコミットすら不要になる（GitHubの「Rebase and merge」ボタンでも同じことができる）。

**使い分けの原則**: 誰かと共有済み・pushしてレビュー中のブランチ、あるいは`main`のような共有ブランチへの取り込みには **merge**。まだ自分しか見ていないローカルの作業ブランチの中で最新化したいだけなら **rebase**（pushしてPRレビューが始まった後は、そのブランチをrebaseしない）。

### 歴史を一直線に保つ利点

- **`git log`/`git blame`が読みやすい**: マージコミットが挟まると、その変更が本流にいつ実際に入ったのかをグラフを目で辿らないと分からない
- **`git bisect`（バグ原因コミットの二分探索）が効率的**: 履歴が線形なら単純な二分探索で済むが、マージが複雑に絡むと分岐を辿る必要が出て効率が落ちる
- **`git revert`がしやすい**: 通常のコミットは1コマンドで取り消せるが、マージコミットの取り消しはどちらの親を基準にするか(`-m`オプション)を指定する必要があり手順が煩雑

小規模なこのプロジェクトなら、GitHub PRの「Squash and merge」を使う運用も現実的。1PR=1コミットにまとまるので、rebaseを手動で意識しなくてもmainの履歴が自然と読みやすく保てる。
