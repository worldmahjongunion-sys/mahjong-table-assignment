# 大会管理とゲスト共有リンク実装依頼（Stage 1）

## 0. 実装前に読んでほしいファイル

- `仕様書.md`（特に2章用語、3.2〜3.6データモデル、4章卓組みロジック仕様、6章非機能要件、7章点数算出ルール）
- `開発ロードマップ.md`・`基準.md`（現状サマリ）
- `table_logic.py`（卓組みロジック本体。関数のシグネチャ・戻り値をそのまま参照して、本依頼の「回戦実行」処理から呼び出すこと）
- `scoring_logic.py`（得点・ポイント集計ロジック。`uma_config`・`point_table`の構造をそのまま大会の評価方式パラメータとして使うこと）
- `db.py`（特に60〜65行目の`_hash_token`/`_generate_token`、188〜203行目の`tenant_invites`テーブルとその周辺関数。ゲスト共有リンクのトークン方式は、この既存パターンを踏襲する）
- `app.py`（519〜622行目の表紙画面。評価方式・パラメータの入力フォームUIをそのまま流用する）

## 1. 背景・目的

これまでメンバー管理・評価方式の集計ロジックまで実装が完了しています。次のステップとして、実際に「大会（道場の1回）」を作成し、卓組みを実行・保存し、成績を記録できるようにしたい（仕様書.md 5章「大会管理」「回戦実施」に相当、これまで未着手）。

あわせて、当面は**特定の友人限定で、アカウント作成なしで試験的に使ってもらいたい**という要望があります。具体的には、大会ごとに「ゲスト用の共有リンク」を発行し、そのリンクを知っている人（LINEグループで共有する想定）は、ログイン・アカウント作成をせずに、その大会の

- 卓組み結果の閲覧
- 成績（素点）の入力（卓ごとに代表者1人が4人分の素点をまとめて入力する想定）
- 順位・累計成績の閲覧

ができるようにしたい。将来的に有料化のめどが立った段階では、正式なアカウント作成必須の形に戻す想定のため、ゲスト共有リンクは主催者側でいつでも無効化できる仕組みにしておく。

**重要**: 今回アカウント作成を不要にしたいのは「ゲスト（参加者）が大会に参加する導線」だけです。メンバー管理・評価方式設定・大会作成などの主催者向け画面は、これまでどおりログイン（アカウント作成）必須のままで変更しません。

規模が大きいため、実装は2段階に分けます。**本依頼はStage 1（データモデル・主催者側の大会管理／回戦実行画面・ゲスト共有リンクの発行の仕組み）のみを対象とします。** ゲスト自身が使う閲覧・入力画面（Stage 2）は別途依頼します。

## 2. データモデル追加（仕様書.md 3.2〜3.6に対応）

以下のテーブルをSQLiteに追加してください（既存の`tenants`/`users`/`members`と同様、`db.py`の`_init_db`相当の箇所にまとめる）。カラム名・型は既存コードの命名規則（snake_case、`created_at`等の命名）に合わせてください。

### 2.1 大会（tournaments）
- id
- tenant_id または user_id（所有アカウント。既存の`members`テーブルのスコープ方式に合わせる）
- 大会名
- 種別: `ワンデー` / `シーズン`
- 卓組み方式: `蛇行` / `ワンデー4半荘`
- 評価方式: `得点` / `ポイント`
- 評価方式パラメータ（JSON等で保持）: `得点`なら`scoring_logic.DEFAULT_UMA_CONFIG`と同じ構造（start_point/return_point/oka/tobi_amount/uma_table）、`ポイント`なら順位ポイント表（4要素配列）
- 選手番号の採番方式: `受付順` / `くじ引き`
- 開始日・終了日（または開催日）
- 状態: `準備中` / `進行中` / `終了`
- 作成日時

### 2.2 大会参加メンバー（tournament_members）
- 大会ID × メンバーID
- 選手番号（大会ごと。`(大会ID, 選手番号)`の複合ユニーク制約）
- 参加登録日時

### 2.3 回戦（rounds）
- 回戦ID、大会ID、回戦番号

### 2.4 卓・着席（tables / seats）
- 卓（回戦IDに紐づく卓番号）
- 着席: 卓ID × メンバーID × スタート位置（東・南・西・北）
- 抜け番: 回戦ID × メンバーID

### 2.5 成績（results）
- 回戦ID × メンバーID × 素点
- 飛び賞の「誰が飛ばしたか」: 回戦ID × 飛んだメンバーID × 飛ばしたメンバーID（複数行で複数人に対応。`scoring_logic.compute_total_score`の`tobi_busters`の構造にそのまま変換できる形にする）
- 総合得点・順位ポイントなど**計算結果自体は保存せず**、素点・飛び賞チェックの生データのみ保存し、表示のたびに`scoring_logic.py`の関数で計算する方針とする（評価方式パラメータを後から見返しても計算過程が再現できるようにするため。table_logic.pyの「乱数シードを保存し結果は再計算しない」方針とは異なる点に注意——こちらは既に確定した卓組み結果なので保存し再利用、成績側は素点という生データから毎回導出、という使い分け）

### 2.6 ゲスト共有リンク（tournament_guest_links）
- id
- 大会ID
- token_hash（`db.py`の`_hash_token`と同じsha256方式。生トークンはDBに保存しない）
- 発行したユーザーID
- 発行日時
- 失効日時（revoked_at、nullable。主催者が「リンクを無効化」した際にセットする）
- `tenant_invites`と異なり、このリンクは「1回使ったら消費される招待」ではなく、大会が続く間ずっと使える共有リンクです。`max_uses`/`use_count`は不要（有効/失効の二値管理のみ）

## 3. 主催者側の画面（ログイン必須。これまでどおり）

仕様書.md 5章「大会管理」「回戦実施」に対応します。

### 3.1 大会管理
- 作成フォーム: 大会名・種別・卓組み方式・評価方式・評価方式パラメータ（現行の表紙画面519〜622行目の入力フォームをそのまま流用）・選手番号採番方式・開始日/終了日
- 一覧・編集・削除
- 参加メンバーの紐付け: 既存メンバー一覧からチェックボックスで選択し、選手番号を採番（受付順は自動採番、くじ引きは手入力。仕様書.md 3.3参照）

### 3.2 回戦実行
- 「次の回戦の卓組みを実行」ボタン
  - これまでの回戦（保存済みの回戦・着席・抜け番レコード）から、`table_logic.py`が必要とする状態（各メンバーの出場回数・直前の抜け番・スタート位置の履歴・同卓履歴など）をDBから再構築し、該当する卓組み方式の関数（蛇行方式／ワンデー4半荘方式）を呼び出す
  - 卓組み方式が「蛇行」の場合は、前回戦までの大会内累計成績（評価方式に応じて累計得点／累計順位ポイントのいずれか。仕様書.md 3章末尾参照）を使う
  - 結果をプレビュー表示 → 「保存」で回戦・卓・着席・抜け番のレコードを作成する
  - これにより、開発ロードマップ.mdに元々あった「Phase 1: 卓組み結果確認用の簡易UI（DB接続なし）」は、本依頼のDB接続ありの実装に置き換わります（重複実装は不要）
- 成績入力フォーム: 卓ごとに素点4人分をまとめて入力（3.2章「入力方式」参照）。素点が0以下の選手には飛び賞チェックボックスを表示し、飛ばした人を選択する（仕様書.md 7.3「誰が飛ばしたか」に対応）

### 3.3 ゲスト共有リンク管理
- 大会詳細画面に「ゲスト用リンクを発行」ボタンを追加
  - 未発行なら新規トークンを生成してリンク（例: 公開URL + `?guest=<トークン>`）を表示、コピーボタンを設置
  - 発行済みならリンクを再表示（既存トークンをそのまま見せる。再生成したい場合は明示的な「リンクを再発行」操作で旧トークンを失効させ新規発行する）
- 「リンクを無効化」ボタン: `revoked_at`をセットし、以後そのトークンでのゲストアクセスを拒否する

## 4. テスト

- 大会・大会参加メンバー・回戦・卓・着席・成績・ゲスト共有リンクの各CRUD操作のユニットテスト（DBアクセス層）
- 回戦実行時の状態再構築ロジック（DBの保存済みレコード→table_logic.pyへの入力形式への変換）のテスト。特に2回戦目以降で、1回戦目の結果が正しく引き継がれること（出場回数・抜け番履歴・同卓履歴・スタート位置履歴）
- ゲスト共有リンクのトークン照合テスト（正しいトークンで大会が特定できる／存在しないトークンや失効済みトークンでは特定できない）
- 成績のtobi_busters変換ロジック（DBの行データ→`scoring_logic.compute_total_score`の`tobi_busters`辞書形式）のテスト

## 5. Definition of Done

- [ ] 2章のテーブルがすべて実装され、既存のマイグレーション方式（`db.py`の初期化処理）に沿って作成される
- [ ] 大会の作成・一覧・編集・削除、参加メンバー紐付けができる
- [ ] 回戦実行（卓組みの実行と保存）ができ、2回戦目以降で前回戦までの状態が正しく引き継がれる
- [ ] 成績入力（卓ごとの素点まとめ入力＋飛び賞チェック）ができる
- [ ] ゲスト共有リンクの発行・表示・無効化ができる（この時点ではゲスト側の閲覧・入力画面自体はまだ実装しない＝Stage 2）
- [ ] メンバー管理・評価方式設定など既存の主催者専用画面が、ログイン必須のまま変更されていないことを確認した
- [ ] 既存のpytestテストスイートが全件green（新規テストを含む）
- [ ] `仕様書.md`に、大会管理・回戦実施のデータモデル確定内容と、ゲスト共有リンクの仕組み（新セクションとして追加、または6章に追記）を反映する
- [ ] `基準.md`・`開発ロードマップ.md`に今回の対応状況を反映する

## 6. 今回のスコープ外（Stage 2以降で対応）

- ゲスト自身が使う画面（トークン付きURLでアクセスした際の、卓組み結果閲覧・成績入力・順位表示のUI）。本依頼はDBとゲストリンクの発行の仕組みまでで、ゲスト向け画面は別依頼とする
- 有料化後の正式なアカウント制への移行フロー自体の設計（今回はリンクの無効化機能を用意するのみ）
- 大会横断集計（Phase 6）
- 3人打ちなど4人打ち以外のルール対応

## 7. 参考：作業の進め方

これまでと同様、`staging`から新しいfeatureブランチを切って作業し、完了後はstagingへのPRを作成してください（マージの実行はユーザーの指示を待ってから）。Stage 1完了後、Stage 2（ゲスト向け画面）の依頼文章を改めて作成します。
