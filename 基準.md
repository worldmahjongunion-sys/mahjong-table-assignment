# 麻雀卓組みアプリ — 基準.md

最終更新: 2026-09-25
状態: Railway公開済み・ログイン付き。大会管理・回戦実施・ゲスト共有リンク（Stage 1）・ゲスト向け画面（Stage 2）・10/2前の修正（#30・#36〜#38）まで、2026-09-25にPR #39で本番（main）へ反映済み（詳細は「2026-09-25: staging→main 昇格」）。以下の「現フェーズ」の各段落は書いた時点の状態のまま

---

## 概要

麻雀の卓組み（メンバーを卓に振り分け、起家等のスタート位置を決める）を自動化するStreamlitアプリ。
サークル会・麻雀共同体WWの大会・火曜日の麻雀道場で使用予定。
卓組み方式は大会ごとに「蛇行（スネーク）方式」「ワンデー4半荘方式」の2種類から選べる（ロジック本体は実装済み、下記参照）。

リポジトリ: https://github.com/worldmahjongunion-sys/mahjong-table-assignment
公開URL: https://app.worldmahjongunion.com（旧URL https://web-production-7d01b.up.railway.app も応答する）

---

## 重要ファイル

| ファイル | 役割 |
|---|---|
| `仕様書.md` | データモデル・卓組みロジック仕様の正本 |
| `開発ロードマップ.md` | 実装フェーズの計画（Phase 0の卓組みロジックが最優先） |
| `Phase0テストケース.md` | 卓組みロジック（Phase 0）のテストケース設計（承認済み・実装前） |
| `基準.md` | このファイル（案内） |

---

## 現フェーズ

Phase 2（メンバー管理CRUD）＋ログイン機能（アカウント登録・招待コード制・ユーザーごとのメンバー管理）まで実装済みで、Railwayに公開済み。加えてPhase 0（卓組みロジック）・評価方式の集計ロジック・Phase 3〜5相当（大会管理・回戦実施・大会内成績表示・ゲスト共有リンク発行、Stage 1）は`staging`にマージ済み（PR #28）。ゲスト向け画面（Stage 2）も実装済みだが、こちらは`feature/guest-screens`ブランチ上でありRailwayには未公開。

**Phase 0（卓組みロジック本体：蛇行方式・ワンデー4半荘方式）は実装済み。** `table_logic.py` にStreamlit/DBから独立した純粋関数として実装し、`Phase0テストケース.md` の全29ケース相当のpytest（`tests/test_table_logic.py`）がgreen。次の最優先タスクはPhase 1（卓組み結果確認用の簡易UI）。

**評価方式（得点／ポイント）の集計ロジック・表紙画面は実装済み。** scoring_logic.pyに①得点評価（ウマ・オカ・飛び賞）②ポイント評価（順位ポイント）の集計ロジックを純粋関数として実装し、ログイン後トップに評価方式選択の表紙画面（app.py）を追加した（得点ポイント評価方式実装依頼.md対応、`feature/cover-screen-scoring`ブランチ、`staging`マージ済み）。

**大会管理・回戦実施・ゲスト共有リンクの発行（Stage 1）は実装済み・staging済み。** `大会管理とゲスト共有リンク実装依頼.md`対応。大会CRUD・参加メンバー紐付け・回戦実行（卓組みの実行と保存、2回戦目以降の状態引き継ぎ）・成績入力（素点＋飛び賞チェック）・大会内順位表・ゲスト共有リンクの発行/再発行/無効化までを実装し、pytest 188件がgreenを確認のうえPR #28で`staging`にマージ済み（マージ後のstagingでも188件green再確認済み）。詳細は仕様書.md 3.7・5章・6章、開発ロードマップ.md Phase 3〜5参照。

**ゲスト向け画面（Stage 2）は実装済み・staging未マージ。** `ゲスト向け画面実装依頼_Stage2.md`対応。`feature/guest-screens`ブランチで、`?guest=<トークン>`付きURLでのログイン不要な専用画面（卓組み結果閲覧、成績入力、大会内順位表閲覧）を実装。実装前確認で判明した2点（ポイント方式の大会が開始点数を持たない、成績レコードに入力日時・入力経路の記録がない）を踏まえ、ポイント方式にも開始点数のみ追加、`results`/`tobi_busters`に`created_at`/`input_source`列を追加、ゲストの成績入力は新規`submit_guest_round_results`（新規INSERTのみ、DBのUNIQUE制約で同時送信の後勝ちを防止）を新設。pytest 203件（188+新規15件）がgreen。このブランチはまだpush・PR作成前（ユーザーの指示待ち）。詳細は仕様書.md 3.8、開発ロードマップ.mdの該当セクション参照。

---

## 次のアクション

- [x] Phase 0: 蛇行方式・ワンデー4半荘方式の卓組みロジックをPython関数として実装
- [x] Phase 0: pytestでユニットテストを書く（人数パターン網羅）
- [ ] Phase 1: 卓組み結果確認用の簡易UI（DB接続なし、目視確認用。Phase 3〜5がDB接続込みで先行実装されたため、実質的に不要になった可能性あり）
- [x] 評価方式（得点／ポイント）の集計ロジック（scoring_logic.py）・表紙画面（app.py）を実装済み
- [x] Phase 2: メンバー管理（登録・一覧・編集・削除）を実装済み
- [x] ログイン機能（アカウント作成・招待コード・ユーザーごとのメンバースコープ）を実装済み
- [x] Railwayへの公開設定を実装済み
- [x] Phase 3〜5相当: 大会管理・回戦実施・大会内成績表示（PR #28、stagingマージ済み）
- [x] ゲスト共有リンクの発行・再発行・無効化（主催者側、Stage 1。同PR）
- [x] ゲスト自身が使う閲覧・入力画面（Stage 2、`feature/guest-screens`ブランチ）
- [x] `feature/guest-screens`をpush・stagingへPR作成（PR #29でstagingにマージ済み、2026-09-18）

### 2026-09-22 にやったこと（10/2 の本番運用に向けて）

2026-09-21 時点: PR #30（順位ポイントの卓ごと計算・主催者画面の合計チェック・100点単位の入出力）と PR #32（リリース手順.md の更新）を staging にマージ済み。PR #31（API）は 10/2 のリリースが終わるまで OPEN のまま置く。PR #35（次のアクションの更新）もstagingにマージ済み（先頭コミット `0fc7a1f`）。

1. [x] Railway の staging（web-staging-1395）に PR #30 が反映されたか確認する
2. [x] staging で LINE 内ブラウザからのゲスト入力を確認する
3. [x] staging で自動翻訳による表示崩れを確認する（issue #34）
4. [x] 問題なければ staging→main の PR を作成する（10/2 の本番運用に向けて）→ PR #39 で main へ反映済み（2026-09-25、下記）

### 2026-09-23: staging(0fc7a1f)実機確認結果とissue #34への対応

Androidの LINE 内蔵ブラウザから、staging（web-staging-1395、コミット `0fc7a1f`）にゲスト共有リンクでアクセスして実機確認した。

- ゲスト入力・PR #30（卓ごとの順位計算）・スマホ表示（375〜430px相当）は**いずれも問題なし**。
  **PR #27（クローズ済み）の「スマホ表示はコード変更不要」という判断は、この実機確認で裏付けられた。**
- 「翻訳しますか？」の表示は出ず、**issue #34はこの端末（Android LINE内蔵ブラウザ）では再現しなかった**
- 見つかった表示上の改善点（`fix/display-polish-before-release`ブランチで対応、PR作成済み・マージは指示待ち）:
  1. 席の表示順を東→南→西→北に統一（`table_logic.py`に表示専用の`position_sort_key`を追加。DBの保存内容・卓組みロジック自体は変更なし）
  2. ログイン画面・「飛ばした人」選択欄などStreamlit既定の英語表示を日本語化
  3. `<html lang="ja">`・`translate="no"`をJS経由で設定（issue #34の予防策。Streamlit本体のHTMLシェルはこのリポジトリで編集できないための次善策。翻訳を完全に禁止する保証はない）
  4. 順位表の列名「値」を評価方式に応じて「総合得点」/「合計ポイント」に変更
- issue #34自体はクローズせず、上記3を予防策として実施した上でオープンのまま残す（実機で再現しなかったこと、および用語統一の残課題があるため）

### 2026-09-25: staging→main 昇格（PR #39）

`安全チェックリスト.md` の手順（バックアップ→staging確認→本番）で、staging（`c2e22d5`）を main へ昇格した。main は `bb505cb`（PR #24）→ `3964b5b`（PR #39 のマージ）。

- **含めたPR**: #25・#26・#28・#29・#30・#32・#35・#36・#37・#38。PR #31（API）は含めず、10/2 のリリース後まで OPEN のまま
- **staging の実機確認（`c2e22d5`）**: PR #37（ゲスト送信の合計チェック）、PR #38 の A（主催者の保存でゲストの入力を上書きしない）・B（主催者はゲストの入力を修正できる）がいずれも合格
- **マージ前の確認**: `c2e22d5` で pytest 276件 green、PR #39 の CI（test）pass、競合なし。本番DBへの変更は表と列を足すだけ（新しい表9つ、新しい表 `results`/`tobi_busters` への列4つ。`db.py` の差分で消えた行は0行、既存の表への `DROP`/`DELETE`/作り直しはない）
- **バックアップ**: 本番Consoleで SQLite のバックアップ機能を使い `/data/backup_20260925.db` を作成（`integrity_check` = ok）してダウンロードし、手元（リポジトリ・OneDrive の外）に保管。`/data` には `mahjong.db` だけ（`-wal`/`-shm`/`-journal` なし）。`/data/backup_20260925.db` も本番の `/data` に残してある
- **基準値（バックアップを手元で読み取り専用で開いて記録）**: `tenants` 3 / `users` 3 / `members` 7 / `audit_logs` 0 / `rate_limit_events` 0 / `email_verification_tokens` 0 / `password_reset_tokens` 0 / `tenant_invites` 0
- **戻し先の確認**: マージ前に、本番 Deployments で `bb505cb` のデプロイに Redeploy があることを確認
- **反映後の確認**: 本番URL・`/_stcore/health` が HTTP 200 / ok（新旧両URL）。本番に主催者アカウントでログインし、メンバー一覧・大会管理の画面が表示された（大会は0件）
- 想定外だったこと（Console の貼り付け、ファイルブラウザの切断、Backups タブ）は `リリース手順.md` の「本番DBのバックアップと確認（2026-09-25の記録）」に書いた

**次のアクション**
- [ ] 10/2 の実運用
- [ ] 10/2 の後: PR #31（API）、`feature/background-email`、`feature/cache-reads`（`save_round_results` の競合解消が必要）を staging へ

---

## 続きを頼む時の一言

「麻雀卓組みアプリの続きをやりたい」と伝えれば、これまでの経緯（仕様書・ロードマップの内容）を踏まえて再開できる。
