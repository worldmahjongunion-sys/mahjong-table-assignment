# 麻雀卓組みアプリ — 基準.md

最終更新: 2026-08-06
状態: Railway公開済み・ログイン付き

---

## 概要

麻雀の卓組み（メンバーを卓に振り分け、起家等のスタート位置を決める）を自動化するStreamlitアプリ。
サークル会・麻雀共同体WWの大会・火曜日の麻雀道場で使用予定。
卓組み方式は大会ごとに「蛇行（スネーク）方式」「ワンデー4半荘方式」の2種類から選べる（ロジック本体は実装済み、下記参照）。

リポジトリ: https://github.com/worldmahjongunion-sys/mahjong-table-assignment
公開URL: https://web-production-7d01b.up.railway.app

---

## 重要ファイル

| ファイル | 役割 |
|---|---|
| `仕様書.md` | データモデル・卓組みロジック仕様の正本 |
| `開発ロードマップ.md` | 実装フェーズの計画（Phase 0の卓組みロジックが最優先） |
| `Phase0テストケース.md` | 卓組みロジック（Phase 0）のテストケース設計（承認済み・実装前） |
| `基準.md` | このファイル（案内） |

---

## 現フェーズ

Phase 2（メンバー管理CRUD）＋ログイン機能（アカウント登録・招待コード制・ユーザーごとのメンバー管理）まで実装済み。①登録②保存③一覧④編集・削除（論理削除）が動作する状態で、Railwayに公開済み。

**Phase 0（卓組みロジック本体：蛇行方式・ワンデー4半荘方式）は実装済み。** `table_logic.py` にStreamlit/DBから独立した純粋関数として実装し、`Phase0テストケース.md` の全29ケース相当のpytest（`tests/test_table_logic.py`）がgreen。次の最優先タスクはPhase 1（卓組み結果確認用の簡易UI）。

**評価方式（得点／ポイント）の集計ロジック・表紙画面は実装済み。** scoring_logic.pyに①得点評価（ウマ・オカ・飛び賞）②ポイント評価（順位ポイント）の集計ロジックを純粋関数として実装し、ログイン後トップに評価方式選択の表紙画面（app.py）を追加した（得点ポイント評価方式実装依頼.md対応）。大会作成・回戦実施の画面への実接続はPhase 3・4で行う。

**スマホ表示は確認済み・変更不要だった。** LINEグループでの配布を見据え、スマホ表示改善依頼.mdの候補4箇所（評価方式ボタン・ウマ表/順位ポイント表の入力欄・会員一覧の並び替え/エクスポート・会員1行の表示）をChrome DevToolsで幅375px/430pxを確認。streamlit 1.60の`st.columns`が狭い画面幅で自動的に縦積みに折り返すため、いずれも崩れ・窮屈さ・見切れは見られず、コード変更は行わなかった（長い氏名・メモでの負荷テスト、横スクロール有無、サイドバー開閉も確認済み）。LINE内蔵ブラウザでの実機確認はこの開発環境では行えていない。

---

## 次のアクション

- [x] Phase 0: 蛇行方式・ワンデー4半荘方式の卓組みロジックをPython関数として実装
- [x] Phase 0: pytestでユニットテストを書く（人数パターン網羅）
- [ ] Phase 1: 卓組み結果確認用の簡易UI（DB接続なし、目視確認用）
- [x] 評価方式（得点／ポイント）の集計ロジック（scoring_logic.py）・表紙画面（app.py）を実装済み
- [x] スマホ表示確認（幅375〜430px）— 問題なし、変更不要。LINE内蔵ブラウザでの実機確認は未実施
- [x] Phase 2: メンバー管理（登録・一覧・編集・削除）を実装済み
- [x] ログイン機能（アカウント作成・招待コード・ユーザーごとのメンバースコープ）を実装済み
- [x] Railwayへの公開設定を実装済み

---

## 続きを頼む時の一言

「麻雀卓組みアプリの続きをやりたい」と伝えれば、これまでの経緯（仕様書・ロードマップの内容）を踏まえて再開できる。
