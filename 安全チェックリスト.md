# 安全チェックリスト：バックアップ → staging検証 → 本番

## 事前準備

1. 変更をブランチで実装し、pytest全件パスを確認する
2. Procfileを変更する場合は、本番・staging両方のRailway Settings → Deploy → Custom Start Command を確認し、Procfileと一致（または未設定）であることを確認する
   （参考：トラブル復旧ガイド.md「4. デプロイは成功しているのにマイグレーションが効かない場合」）

## バックアップ

3. 本番の /data （DBファイル）を、RailwayのConsole下部のファイルブラウザ（feature flag「Priority Boarding」で有効化）からダウンロードし、日付入りのファイル名で保管する
   （Trialプランでは「Backups」タブはPro plan限定のため使用不可。Railway CLIがOSのアプリケーション制御ポリシー等でブロックされる環境では、ブラウザのファイルブラウザ機能が代替手段になる）

## staging検証

4. PRをstagingへ作成・マージする
5. Deploymentsタブで最新デプロイのコミットハッシュが意図したものと一致しているか確認する
6. アプリを一度も開く前に、Consoleでスキーマ変更（例：新しい列）と既存データの件数・内容が壊れていないかを確認する

## 本番適用

7. staging→main昇格PRを作成・マージする
8. 本番のDeploymentsタブで最新デプロイのコミットハッシュを確認する
9. staging同様、アプリを開く前に本番Consoleでスキーマ・データを再確認する（stagingで確認済みでも本番で必ず繰り返す）

## 異常時

10. 本番で問題が見つかった場合は、③でダウンロードしたバックアップファイルをファイルブラウザからアップロードして復元し、再度件数・データを確認する

## 記録

11. 想定外の落とし穴が見つかった場合は、トラブル復旧ガイド.md／リリース手順.mdに追記する
