# スマホ表示改善依頼

## 0. 実装前に読んでほしいファイル

- `基準.md`（プロジェクトの現状サマリ）
- `開発運用の型.md`（ブランチ運用ルール）
- `app.py`（今回の対象ファイル。特に以下の行を参照）
  - 16行目: `st.set_page_config(...)`（`layout`引数が未指定＝デフォルトの`"centered"`）
  - 519〜527行目: 表紙画面の評価方式選択ボタン（`st.columns(2)`）
  - 538〜618行目: 評価方式の設定フォーム（ウマ表・順位ポイント表の入力。574行目・604行目で`st.columns(4)`を使用）
  - 752行目・766行目: 会員一覧の並び替え・エクスポートボタン（`st.columns([2, 2])` / `st.columns(2)`）
  - 796〜844行目: 会員一覧の1行（`st.columns([3, 4, 2, 2])`。氏名・メモ・編集・削除を横並び表示）
- `requirements.txt`（`streamlit==1.60.0`を使用中）

## 1. 背景・目的

現在このアプリは、大会運営者がPCのブラウザから使うことを主に想定して作られています。
今後、公開URL（Railway上の本番環境）を**LINEのグループチャットに貼り付けて配布**し、参加者がスマホから開いて使う運用を予定しています。

LINEでリンクを共有した場合、タップした相手の多くは**LINEアプリ内蔵のブラウザ（LINE内ブラウザ／WebView）**でページを開きます。これは通常のスマホのSafari／Chromeとは表示エンジンや操作感が微妙に異なることがあるため、確認対象に含めてください。

このアプリの新規のスマホ向け画面を作る依頼ではありません。**今すでにある画面を、スマホの狭い画面幅で開いたときにも問題なく使えるように調整する**ことが目的です。

## 2. 対象範囲

### 対象（スコープ内）

- 現在ログイン後に表示されている画面（ログイン・サインアップ・パスワード再設定画面を含む）を、スマホの画面幅（目安：幅375〜430px程度。iPhone SE〜iPhone 15 Pro Max相当）で開いたときの表示・操作性の確認と改善
- 具体的に確認してほしい候補箇所（下記3章）
- 確認手段：ブラウザのモバイルエミュレーション（Chrome DevToolsのデバイスツールバーなど）に加え、可能であれば実機（スマホ）でも確認
- LINE内蔵ブラウザでの表示確認（後述4章）

### 対象外（スコープ外）

- まだ実装されていないPhase 3・4（大会管理・回戦実施）画面のモバイル対応設計
- レイアウトの全面的な作り直しやデザインの刷新（配色・フォント変更など）
- PC版の見た目・挙動を変更すること（スマホでの見え方だけを改善し、PC幅で見たときの現状のレイアウトは崩さないこと）
- ネイティブアプリ化やPWA化

## 3. 確認・改善してほしい項目

以下は、コードを読んだ時点で「スマホの狭い画面幅では窮屈になりそうだ」と見える候補箇所です。実際にモバイル幅で表示を確認した上で、崩れ・窮屈さ・押しにくさが確認できたものを改善してください（見た目上問題なければ変更不要です。憶測だけで全面的に書き換えないでください）。

1. **表紙画面の評価方式ボタン**（519〜527行目 `st.columns(2)`）
   「①得点で評価」「②ポイントで評価」の2つのボタンが横並びになる。スマホ幅でボタン内のテキストが折り返されて押しにくくなっていないか確認。

2. **評価方式の設定フォーム内、ウマ表・順位ポイント表の入力欄**（574行目・604行目 `st.columns(4)`）
   1着〜4着ぶんの数値入力欄が4列横並びになる。スマホ幅では1列あたりが非常に狭くなり、数値が見切れたり±ボタン（`st.number_input`の増減ボタン）が押しにくくなっている可能性が高い候補箇所。必要であれば、スマホ幅のときだけ2列×2行などに折り返す、もしくは列数を減らす等の対応を検討してください。

3. **会員一覧の並び替え・エクスポートボタン**（752行目・766行目）
   こちらも横並びのボタン・セレクトボックスがスマホ幅で窮屈になっていないか確認。

4. **会員一覧の1行の表示**（796〜844行目 `st.columns([3, 4, 2, 2])`）
   氏名・メモ・編集ボタン・削除ボタンが1行に横並びになる。スマホ幅では特にメモ欄や氏名が見切れたり、編集・削除ボタンが小さくなりすぎて誤タップを誘発しないか確認。

5. **その他、上記以外でも実際にスマホ幅で表示した際に窮屈・崩れが見つかった箇所**があれば、範囲内で合わせて改善して構いません（ただし2章の対象外事項には触れないこと）。

## 4. テスト・確認方法

- Chrome DevToolsのデバイスツールバー等で、幅375px程度（狭いスマホ相当）・幅430px程度（大きめのスマホ相当）の両方で一通りの画面（ログイン→表紙画面→評価方式選択→設定フォーム→会員一覧）を確認する
- 可能であれば実機のスマホブラウザ（Safari／Chrome）でも同様に確認する
- **可能であればLINE内蔵ブラウザでも動作確認する**（LINEで自分宛てなどに公開URLを送り、タップして開く。LINE内蔵ブラウザ特有の表示崩れや操作不能な箇所がないか）。LINE内蔵ブラウザでの確認が環境的に難しい場合は、その旨を報告してもらえれば構いません（必須のブロッカーにはしません）
- 自動テスト（pytest）で検証できる性質の変更ではないため、既存のテストスイートがgreenのまま壊れていないことのみ確認する（表示崩れの検証は目視確認が中心になります）

## 5. Definition of Done

- [ ] 3章の候補箇所を、スマホ幅（375〜430px程度）のブラウザ表示で確認した
- [ ] 表示・操作上の問題が見つかった箇所を改善した（問題がなければ「確認したが変更不要だった」旨を報告）
- [ ] PC幅（既存のcenteredレイアウト）での見た目・操作性が変わっていないことを確認した
- [ ] 既存のpytestテストスイートが全件green
- [ ] `基準.md`・`開発ロードマップ.md`に今回の対応状況を反映（該当箇所があれば）
- [ ] 対応前に本ドキュメントの対象範囲（2章）を超える変更が必要だと感じた場合は、先に報告して確認を取ってから進める

## 6. 参考：作業の進め方

これまでと同様、`staging`から新しいfeatureブランチを切って作業し、完了後はstagingへのPRを作成してください（マージの実行はユーザーまたはユーザーの指示を待ってから）。
