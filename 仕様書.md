# 麻雀卓組みアプリ — 仕様書

最終更新: 2026-08-06
状態: メンバー管理・ログイン機能はRailwayに公開済み。卓組みロジック本体（Phase 0）は未実装

---

## 1. 概要

サークル会・麻雀共同体WWの大会・火曜日の麻雀道場で使う、麻雀の卓組み（メンバーを卓に振り分け、起家等のスタート位置を決める）を自動化するStreamlitアプリ。

卓組み方式は大会ごとに以下の2種類から選べる（いずれもロジック本体は未実装。仕様のみ）。

1. **蛇行（スネーク）方式** — サークル会・火曜の麻雀道場で使用。前回戦までの成績順に並べ、蛇行（ジグザグ）で卓に振り分ける。
2. **ワンデー4半荘方式** — ワンデー大会で使用。4半荘を通して同じ人と2回当たらず、各自が東家・南家・西家・北家スタートを1回ずつ経験する。

卓組みロジックの正確さが最優先事項であり、UI・保存機能はその後に肉付けする方針だったが（詳細は「開発ロードマップ.md」）、実際にはメンバー管理・ログイン機能・Railway公開が先行して実装済み。Phase 0（卓組みロジック）は次の最優先タスクとして未着手のまま残っている。

---

## 2. 用語

| 用語 | 意味 |
|---|---|
| アカウント（主催者） | ログイン機能で作成する主催者用アカウント。招待コードを知っている者のみ新規登録できる |
| メンバー | 大会に参加する個人。同一アカウント内の全大会で共有されるマスタデータ（アカウントごとに独立して管理される） |
| 大会 | ワンデー型またはシーズン型の開催単位。参加メンバーは大会ごとに異なる |
| 選手番号 | 大会ごとに参加者へ割り振る番号。メンバーIDとは別物で、大会をまたいで引き継がない |
| 回戦（半荘） | 大会内の1回の対局単位。各回戦で卓組みが行われる |
| 卓 | 1回戦の中で4人が着席する単位 |
| 起家 | 卓内でのスタート位置（東家・南家・西家・北家）のうち東家 |
| 抜け番 | 参加人数が4の倍数でないときに、その回戦を欠場するメンバー |

---

## 3. データモデル

### 3.0 アカウント（実装済み）
- ユーザーID
- ユーザー名（英数字とアンダースコア、3〜30文字。小文字に正規化して保存）
- パスワードハッシュ（bcrypt、streamlit-authenticator経由）
- 登録日時
- 新規登録には招待コード（合言葉）の入力が必要。招待コードは環境変数または`.streamlit/secrets.toml`で設定し、コード自体はDBに保存しない
- ログインセッションはCookie（有効期限30日）で維持

### 3.1 メンバー（アカウントごとに共有）
- メンバーID
- ユーザーID（所有アカウント。同一アカウント内の全大会で共有される）
- 名前
- メモ（任意）
- 登録日時
- 有効フラグ（is_active）: 論理削除用。過去の大会・対局記録がメンバーIDに紐づくため、削除は物理削除ではなく本フラグをOFFにする「引退」扱いとする。一覧はデフォルトで有効メンバーのみ表示し、「引退メンバーも表示」で再表示・復帰できる

### 3.2 大会
- 大会ID
- 大会名
- 種別: `ワンデー` / `シーズン`
- 卓組み方式: `蛇行` / `ワンデー4半荘`
- 選手番号の採番方式: `受付順` / `くじ引き`（大会ごとに選択。3.3参照）
- 開始日・終了日（シーズン型は期間、ワンデー型は開催日）
- 状態: `準備中` / `進行中` / `終了`

### 3.3 大会参加メンバー
- 大会ID × メンバーID の紐付け
- 選手番号: 大会ごとに参加者へ割り振る番号。メンバーIDとは別物で、大会をまたいで使い回さない。同点時のタイブレーク（4.2参照）に使うため、大会参加登録の一部として持たせる
  - 採番方式は大会ごとに「受付順」「くじ引き」のいずれかを選ぶ（3.2参照）
  - **受付順**の場合: 参加登録のたびに大会ごとのカウンタを1つ増やして自動採番する
  - **くじ引き**の場合: くじに印字された番号をそのまま選手番号として手入力する（引いた順の連番ではない）。そのため選手番号は連番であることも1から始まることも前提にできず、自動採番ではなく手入力を受け付けるフィールドとして設計する
  - 採番方式によらず、同一大会内で選手番号が重複しないよう、DB上は`(大会ID, 選手番号)`の複合ユニーク制約で担保する（くじ引き方式は手入力のため、特に重複ミスを構造的に防ぐ必要がある）
- 参加登録日時

> **設計メモ（選手番号の配置）**: 選手番号は「あるメンバーがある大会に何番目として参加したか」という文脈でのみ意味を持ち、メンバー本体（3.1）の恒久的な属性ではない。そのため大会とメンバーの結合エンティティである本テーブル（3.3）に持たせるのが適切。

### 3.4 回戦（半荘）
- 回戦ID
- 大会ID
- 回戦番号（第何回戦か）

### 3.5 卓・着席
- 卓ID（回戦IDに紐づく、卓番号）
- 着席: 卓ID × メンバーID × スタート位置（東南西北）
- 抜け番: 回戦ID × メンバーID（その回戦を欠場したことを記録）

### 3.6 成績
- 回戦ID × メンバーID × 順位 × 点数

成績は大会ごとに独立してカウントする。メンバーIDで紐づけているため、将来的な大会横断集計（Phase 6、ロードマップ参照）にも対応できる設計とする。

> **成績集計ルール（確定）**: 蛇行方式で「前回戦までの成績順」を出す際の集計ルールは「合計ポイントの降順」とする。4.1の抜け番均等化ルール（出場回戦数が最多の人から抜ける）により大会を通じた出場回戦数の差が抑えられるため、合計点数ベースでも出場回数の少なさによる不公平が生じにくいことを前提とする。合計点数が同点だった場合の並び順は選手番号の若い順とする（4.2手順2参照）。なお、ウマ・オカの有無など、点数そのものの算出方法（成績入力時の点数計算ルール）は別途未確定（7章参照）。

---

## 4. 卓組みロジック仕様

両方式共通の入力: `メンバーリスト（N人）`
共通の出力: `卓ごとのメンバー割当 + スタート位置（東南西北）` + `抜け番リスト`

> **再現性・保存の方針**: 卓組みロジックは、抜け番選出のタイブレーク、蛇行方式の第1回戦仮順位、ワンデー方式の半荘1のランダム割当や貪欲法の候補生成など、乱数を使う箇所を含む。これらはすべて乱数シードを外部から注入できる設計とし、同一シード・同一入力からは必ず同一の卓組み結果が再現されることを保証する。実運用では、一度確定した卓組み結果をDB（3.5 卓・着席）に保存し、以後の画面再表示はロジックを再実行せず保存済みの結果を用いる。

### 4.1 抜け番ルール（共通）

- 卓数 `T = floor(N / 4)`、抜け番人数 `R = N mod 4`
- `R > 0` の場合、`R`人を抜け番とする
- 抜け番は大会を通して全員に均等に回す。具体的には「その大会でのそれまでの出場回戦数」が最も多い人たちの中から`R`人を選出する（出場回戦数で比較することで、途中参加者が真っ先に抜け番に選ばれてしまう問題を避ける）
- 出場回戦数が同数で並ぶ場合は、直前の回戦で抜け番だった人を候補から除外したうえでランダムに選ぶ（同じ人が2回連続で抜け番になることを避けるため）。ただし除外すると候補が`R`人に満たなくなる場合は、除外せず同数の全員を対象にランダムに選ぶ
- 抜け番の回戦は欠場として扱い、成績には影響させない（順位・点数の算入なし）
- スタート位置（東南西北）の1周ノルマは、大会を通して「実際に出場した回戦の中で」1巡すればよいものとする（抜け番になった回戦はノルマの対象外）

### 4.2 蛇行（スネーク）方式

**入力**: 現在の大会内順位（1位〜N位。抜け番を除いた出場者のみ）

**手順**:
1. 第1回戦（まだ成績がない）の場合は、出場メンバーをランダムに並べて仮の「順位」とする
   - 過去の他大会での通算成績データがあれば、参考情報として画面に表示する程度に留め、初期シードには使わない（初回起動時や初参加者がいる場合に成立しなくなるため）
2. 第2回戦以降は、前回戦までの大会内累計成績順（3章末尾の集計ルールを参照）を使う。合計点数が同点の場合は選手番号の若い順を優先する。この並び順は、手順3の卓割当・手順4の卓内着席順のいずれにも共通して適用する
3. 卓数 `T` に対して、順位を `T`人ずつのブロックに分割し、ブロックごとに割り当て方向を交互に反転させる（ボウストロフェドン／蛇行配分）
   - 例: T=4（16人）の場合
     - 1〜4位: 卓1=1位, 卓2=2位, 卓3=3位, 卓4=4位（順方向）
     - 5〜8位: 卓4=5位, 卓3=6位, 卓2=7位, 卓1=8位（逆方向）
     - 9〜12位: 卓1=9位, 卓2=10位, 卓3=11位, 卓4=12位（順方向）
     - 13〜16位: 卓4=13位, 卓3=14位, 卓2=15位, 卓1=16位（逆方向）
     - 結果: 卓1={1,8,9,16}, 卓2={2,7,10,15}, 卓3={3,6,11,14}, 卓4={4,5,12,13}（要件の例と一致）
   - Nが4の倍数でない場合は、4.1の抜け番ルールで抜け番を先に決めてから、残りの出場者に対して上記の蛇行配分を行う
4. 各卓内のスタート位置（起家・南家・西家・北家）は、卓組みに使った順位（大会内トータルスコア順、または第1回戦は仮順位）の上位から「東家 → 南家 →西家 → 北家」の順に割り当てる
   - 例: 卓1={1位, 8位, 9位, 16位} の場合、東家=1位, 南家=8位, 西家=9位, 北家=16位
   - 第1回戦はスコアがないため、手順1でランダムに並べた仮順位をそのまま使う
   - このルールは蛇行方式に固有。ワンデー4半荘方式では「各自が東南西北を1回ずつ経験する」ことを優先するため、スコア順の着席は使わない（4.3参照）

### 4.3 ワンデー4半荘方式

**入力**: 参加メンバーリスト（N人）

**制約と優先順位（確定）**:
- 各自が4半荘のうち、実際に出場した回戦の範囲内で東家・南家・西家・北家を1回ずつ経験する（**必達制約・最優先**。蛇行方式のようなスコア順の着席は行わない）
- 4半荘を通して、同じ卓に同席したことのある2人の組み合わせを再度同席させない（**努力目標**。上記の席順一巡と両立しない場合は、席順一巡を優先し、同卓重複を許容する）
- 抜け番は4.1のルールに従い、大会（＝4半荘）を通して全員に均等に回す

**アルゴリズム方針**:
- 人数が16人・20人など、数学的に「4半荘とも全員が別の相手」を厳密に満たす組み合わせが存在する場合でも、事前定義済みの組み合わせ表は持たない（人数パターンごとの保守コストが高いため）
- 汎用的な生成アルゴリズムを採用する:
  1. 半荘1: メンバーをランダムに卓へ振り分け、スタート位置もランダムに割当
  2. 半荘2〜4: 各人の「まだ経験していない位置」を記録しておく。卓分けの際は、各卓に集まる4人の「未経験位置」の組み合わせが東南西北の4席にちょうど割り当てられる（＝席順一巡を成立させられる）組み合わせを優先して探索する
  3. 席順一巡を成立させられる卓分けが複数ある場合は、その中から「これまでに同席した回数」ができるだけ少ないペアが同卓になるものを貪欲法で選ぶ（候補を複数パターン生成し、同席重複数が最小のものを採用する等のランダムリスタート＋局所探索）
  4. 席順一巡を成立させられる卓分けが存在しない場合は、席順一巡の成立を優先し、同卓重複が増えることを許容してでも全員の席順一巡を成立させる卓分けを採用する
- 人数の都合で完全な重複ゼロが不可能な場合（4人・5人・8人・12人など、4半荘分の延べ対戦組数がペア総数を上回ってしまうケース）は、席順一巡が成立していることを前提に、重複数が最小になる近似解を採用する。対応人数を限定したり、エラーで止めたりはしない
- 重複数が同じ卓分け候補が複数生成された場合は、選手番号の小さい人がより若い卓番号に入る候補を優先して一意に決定する（選手番号の昇順に候補同士を比較し、最初に卓番号の差が生じた時点でより小さい卓番号を持つ候補を採用する）

**検証方法**: Phase 0（ロードマップ参照）でユニットテストを書き、代表的な人数（4, 8, 12, 16, 20, 24, 4の倍数でない例として14, 18など）で「スタート位置が出場回戦内で必ず1巡しているか（必達制約）」「その制約の下で同卓重複ペア数ができるだけ少ないか」「抜け番が均等に回っているか」を検証する。

---

## 5. 画面・機能要件

大会・メンバー・回戦それぞれについて、フォーム→保存→一覧→編集・削除のサイクルを実装する（詳細な段階分けは開発ロードマップ.md）。

- ログイン・新規登録（実装済み）: 招待コード制のアカウント新規作成フォーム／ログインフォーム／ログアウト。未ログイン時はメンバー管理画面にアクセスできない
- メンバー管理（実装済み）: 登録フォーム／一覧（登録順・名前順切替）／編集／削除（論理削除＝引退フラグ、確認ダイアログあり、「引退メンバーも表示」で再表示・復帰可）。いずれもログイン中のアカウントにスコープされる
- 大会管理（未実装）: 作成フォーム（名称・種別・卓組み方式・期間）／一覧／編集・削除／参加メンバーの紐付け
- 回戦実施（未実装）: 「卓組みを実行」ボタン→結果表示→保存／回戦一覧／成績入力フォーム／編集・削除
- 大会内成績表示（未実装）: 累計順位表（蛇行方式の次回戦入力にも使用）
- （将来）大会横断集計（未実装）: メンバーごとの複数大会成績サマリー

---

## 6. 非機能要件・技術方針

- フロントエンド／実行環境: Streamlit
- ホスティング: Railway（Procfileで`streamlit run app.py`を起動。公開URL: https://web-production-7d01b.up.railway.app）
- データ永続化: SQLite（大会・メンバー・回戦・成績という関連データを扱うため、CSV/JSONより構造化クエリに向く）
- 想定利用者: 大会主催者（1〜数名）による運用。認証はstreamlit-authenticator＋bcryptによるアカウント制（招待コードを知る者のみ新規登録可）。データはアカウント単位でスコープされる（権限分離は主催者間のデータ分離のみで、ロールベースの権限管理は現時点ではスコープ外）
- 卓組みロジックは、Streamlit UIから独立した純粋関数（入力: メンバーリスト等 → 出力: 卓割当）として実装し、pytest等で単体テスト可能な形にする（Phase 0、未実装）

---

## 7. 未確定事項（実装を進める中で確認）

- 点数算出ルール（ウマ・オカの有無など）の詳細。順位の集計方法自体は「合計ポイント降順・同点は選手番号の若い順」で確定済み（3章末尾・4.2手順2参照）
- 卓は4人打ち固定を前提とする（3人打ちは将来拡張の可能性ありだが現時点ではスコープ外）
