# 麻雀卓組みアプリ — 仕様書

最終更新: 2026-09-18
状態: メンバー管理・ログイン機能はRailwayに公開済み。卓組みロジック本体（Phase 0）・評価方式の集計ロジック・大会管理（回戦実施・ゲスト共有リンクの発行まで、Stage 1）を実装済み

---

## 1. 概要

サークル会・麻雀共同体WWの大会・火曜日の麻雀道場で使う、麻雀の卓組み（メンバーを卓に振り分け、起家等のスタート位置を決める）を自動化するStreamlitアプリ。

卓組み方式は大会ごとに以下の2種類から選べる（いずれもロジック本体を`table_logic.py`に実装済み）。

1. **蛇行（スネーク）方式** — サークル会・火曜の麻雀道場で使用。前回戦までの成績順に並べ、蛇行（ジグザグ）で卓に振り分ける。
2. **ワンデー4半荘方式** — ワンデー大会で使用。4半荘を通して同じ人と2回当たらず、各自が東家・南家・西家・北家スタートを1回ずつ経験する。

卓組みロジックの正確さを最優先事項として先に固め（Phase 0）、次いで評価方式（得点／ポイント）の集計ロジック、大会管理・回戦実施・ゲスト共有リンクの発行（大会管理とゲスト共有リンク実装依頼.md Stage 1）まで実装済み。ゲスト自身が使う閲覧・入力画面（Stage 2）は未着手（詳細は「開発ロードマップ.md」）。

---

## 2. 用語

| 用語 | 意味 |
|---|---|
| アカウント（主催者） | ログイン機能で作成する主催者用アカウント。招待コードを知っている者のみ新規登録できる |
| メンバー | 大会に参加する個人。同一アカウント内の全大会で共有されるマスタデータ（アカウントごとに独立して管理される） |
| 大会 | ワンデー型またはシーズン型の開催単位。参加メンバーは大会ごとに異なる |
| 選手番号 | 大会ごとに参加者へ割り振る番号。メンバーIDとは別物で、大会をまたいで引き継がない |
| 回戦（半荘） | 大会内の1回の対局単位。各回戦で卓組みが行われる |
| 卓 | 1回戦の中で4人が着席する単位 |
| 起家 | 卓内でのスタート位置（東家・南家・西家・北家）のうち東家 |
| 抜け番 | 参加人数が4の倍数でないときに、その回戦を欠場するメンバー |

---

## 3. データモデル

### 3.0 アカウント（実装済み）
- ユーザーID
- ユーザー名（英数字とアンダースコア、3〜30文字。小文字に正規化して保存）
- パスワードハッシュ（bcrypt、streamlit-authenticator経由）
- 登録日時
- 新規登録には招待コード（合言葉）の入力が必要。招待コードは環境変数または`.streamlit/secrets.toml`で設定し、コード自体はDBに保存しない
- ログインセッションはCookie（有効期限30日）で維持

### 3.1 メンバー（アカウントごとに共有）
- メンバーID
- ユーザーID（所有アカウント。同一アカウント内の全大会で共有される）
- 名前
- メモ（任意）
- 登録日時
- 有効フラグ（is_active）: 論理削除用。過去の大会・対局記録がメンバーIDに紐づくため、削除は物理削除ではなく本フラグをOFFにする「引退」扱いとする。一覧はデフォルトで有効メンバーのみ表示し、「引退メンバーも表示」で再表示・復帰できる

### 3.2 大会
- 大会ID
- 大会名
- 種別: `ワンデー` / `シーズン`
- 卓組み方式: `蛇行` / `ワンデー4半荘`
- **評価方式**: `得点` / `ポイント`（大会ごとに選択。7章「点数算出ルール」参照）
  - `得点`の大会は、開始点数・返し点・ウマ表（浮き人数0〜3人分）・オカ・飛び賞額（scoring_logic.pyの`uma_config`一式）を大会ごとに保持する
  - `ポイント`の大会は、順位ポイント表（1〜4位、scoring_logic.pyの`point_table`）を大会ごとに保持する
  - 評価方式パラメータ一式はJSON文字列として`tournaments`テーブルの1カラム（`scoring_config`）に保持する（大会管理とゲスト共有リンク実装依頼.md Stage 1で実装済み）
- 選手番号の採番方式: `受付順` / `くじ引き`（大会ごとに選択。3.3参照）
- 開始日・終了日（シーズン型は期間、ワンデー型は開催日）
- 状態: `準備中` / `進行中` / `終了`
- 大会は所有アカウント（`tenant_id`）にスコープされる（3.1のメンバーと同じスコープ方式）

### 3.3 大会参加メンバー
- 大会ID × メンバーID の紐付け
- 選手番号: 大会ごとに参加者へ割り振る番号。メンバーIDとは別物で、大会をまたいで使い回さない。同点時のタイブレーク（4.2参照）に使うため、大会参加登録の一部として持たせる
  - 採番方式は大会ごとに「受付順」「くじ引き」のいずれかを選ぶ（3.2参照）
  - **受付順**の場合: 参加登録のたびに大会ごとのカウンタを1つ増やして自動採番する
  - **くじ引き**の場合: くじに印字された番号をそのまま選手番号として手入力する（引いた順の連番ではない）。そのため選手番号は連番であることも1から始まることも前提にできず、自動採番ではなく手入力を受け付けるフィールドとして設計する
  - 採番方式によらず、同一大会内で選手番号が重複しないよう、DB上は`(大会ID, 選手番号)`の複合ユニーク制約で担保する（くじ引き方式は手入力のため、特に重複ミスを構造的に防ぐ必要がある）
- 参加登録日時

> **設計メモ（選手番号の配置）**: 選手番号は「あるメンバーがある大会に何番目として参加したか」という文脈でのみ意味を持ち、メンバー本体（3.1）の恒久的な属性ではない。そのため大会とメンバーの結合エンティティである本テーブル（3.3）に持たせるのが適切。

### 3.4 回戦（半荘）
- 回戦ID
- 大会ID
- 回戦番号（第何回戦か）

### 3.5 卓・着席
- 卓ID（回戦IDに紐づく、卓番号）
- 着席: 卓ID × メンバーID × スタート位置（東南西北）
- 抜け番: 回戦ID × メンバーID（その回戦を欠場したことを記録）

### 3.6 成績
- 回戦ID × メンバーID × 素点（`results`テーブル）
- 飛び賞の「誰が飛ばしたか」: 回戦ID × 飛んだメンバーID × 飛ばしたメンバーID（`tobi_busters`テーブル。1人の被飛びに対し複数行で複数の飛ばした人に対応する。`scoring_logic.compute_total_score`の`tobi_busters`引数の形にそのまま変換できる）
- **総合得点・順位ポイントなど計算結果自体は保存しない。** 素点・飛び賞チェックという生データのみをDBに保存し、表示のたびに`scoring_logic.py`の関数で計算する（評価方式パラメータを後から見返しても計算過程を再現できるようにするため）。4.卓組みロジックの「乱数シードを保存し結果は再計算しない」方針とは対照的な使い分けである点に注意——卓組みは既に確定した結果として保存・再利用するが、成績は素点という生データから毎回導出する

成績は大会ごとに独立してカウントする。メンバーIDで紐づけているため、将来的な大会横断集計（Phase 6、ロードマップ参照）にも対応できる設計とする。

> **用語整理（確定）**: 以前は「合計ポイントの降順」という表現を評価方式によらず一括りに使っていたが、7章の点数算出ルール確定に伴い、以下の2つを区別する用語に統一する。
> - **累計得点**: 評価方式が「得点」の大会で使う、素点＋ウマ＋オカ（＋飛び賞）の総和（scoring_logic.pyの`compute_total_score`の累計）
> - **累計順位ポイント**: 評価方式が「ポイント」の大会で使う、各回戦の順位ポイントの総和（scoring_logic.pyの`compute_rank_points`の累計）
>
> **成績集計ルール（確定）**: 蛇行方式で「前回戦までの成績順」を出す際は、大会の評価方式に応じて累計得点／累計順位ポイントのいずれかを使う（4.2手順2を評価方式に応じて一般化したもの。table_logic.pyの`snake_rank`自体の改修は不要で、呼び出し側が渡す`cumulative_scores`を評価方式に応じて使い分ける）。いずれの場合も、集計は「合計降順」、同点だった場合の並び順は選手番号の若い順とする（4.2手順2参照）。
>
> 評価方式が「ポイント」の場合、大会の最終順位は**累計順位ポイント→素点合計→選手番号**の3段階タイブレークで決める（scoring_logic.pyの`rank_by_points_then_score`。2段階目に渡す値は`tournament_service._cumulative_raw_score_sum`が計算する、ウマ・オカを含まない素点の累計合計）。`ポイント`評価方式の大会はそもそもウマ・オカ設定を保持しない（3.2参照）ため、`得点`評価方式の「累計得点」（ウマ・オカ込み）は使わず素点合計で代用する、と正式に決定（1段階目の累計順位ポイントで大半のケースは決着する想定）。点数そのものの算出方法（ウマ・オカ・飛び賞・順位ポイント表の詳細）は7章「点数算出ルール」参照。

### 3.7 ゲスト共有リンク
- 大会ID × トークンハッシュ（`token_hash`。`db.py`の`tenant_invites`と同じsha256方式。生トークンはDBに保存しない）
- 発行者ユーザーID・発行日時・失効日時（`revoked_at`、nullable）
- `tenant_invites`と異なり「1回使ったら消費される招待」ではなく、大会が続く間ずっと使える共有リンク。`max_uses`/`use_count`は持たず、有効／失効の二値管理のみ
- 1大会につき有効なリンクは常に最大1本（新規発行時に旧リンクを明示的に失効させる「リンクを再発行」操作のみが再生成の手段）
- **生トークンの再表示について**: 生トークンはハッシュ化して保存するため、DBから後から復元することはできない。そのため「発行済みリンクの文字列を画面に再表示できる」のは、そのリンクを発行した**その場のブラウザセッション内のみ**（`st.session_state`。招待コード機能の`last_invite_link`と同じ扱い）。別セッションで再ログインした場合は「発行済みだが文字列は再表示できない」旨を表示し、共有し忘れた場合は「リンクを再発行」で旧リンクを失効させて新規発行してもらう。**これは大会管理とゲスト共有リンク実装依頼.mdの原文（「発行済みならリンクを再表示（既存トークンをそのまま見せる）」）から離れた実装判断であり、生トークン非保存という既存のセキュリティ方針を優先した結果である。**
- ゲストがトークン付きURL（`?guest=<トークン>`）でアクセスした際の閲覧・入力画面自体はStage 2で実装する。Stage 1で実装済みなのは、主催者側のリンク発行・再発行・無効化操作と、トークンから大会を特定する照合ロジック（`db.get_tournament_by_guest_token`）まで

---

## 4. 卓組みロジック仕様

両方式共通の入力: `メンバーリスト（N人）`
共通の出力: `卓ごとのメンバー割当 + スタート位置（東南西北）` + `抜け番リスト`

> **再現性・保存の方針**: 卓組みロジックは、抜け番選出のタイブレーク、蛇行方式の第1回戦仮順位、ワンデー方式の半荘1のランダム割当や貪欲法の候補生成など、乱数を使う箇所を含む。これらはすべて乱数シードを外部から注入できる設計とし、同一シード・同一入力からは必ず同一の卓組み結果が再現されることを保証する。実運用では、一度確定した卓組み結果をDB（3.5 卓・着席）に保存し、以後の画面再表示はロジックを再実行せず保存済みの結果を用いる。

### 4.1 抜け番ルール（共通）

- 卓数 `T = floor(N / 4)`、抜け番人数 `R = N mod 4`
- `R > 0` の場合、`R`人を抜け番とする
- 抜け番は大会を通して全員に均等に回す。具体的には「その大会でのそれまでの出場回戦数」が最も多い人たちの中から`R`人を選出する（出場回戦数で比較することで、途中参加者が真っ先に抜け番に選ばれてしまう問題を避ける）
- 出場回戦数が同数で並ぶ場合は、直前の回戦で抜け番だった人を候補から除外したうえでランダムに選ぶ（同じ人が2回連続で抜け番になることを避けるため）。ただし除外すると候補が`R`人に満たなくなる場合は、除外せず同数の全員を対象にランダムに選ぶ
- 抜け番の回戦は欠場として扱い、成績には影響させない（順位・点数の算入なし）
- スタート位置（東南西北）の1周ノルマは、大会を通して「実際に出場した回戦の中で」1巡すればよいものとする（抜け番になった回戦はノルマの対象外）

### 4.2 蛇行（スネーク）方式

**入力**: 現在の大会内順位（1位〜N位。抜け番を除いた出場者のみ）

**手順**:
1. 第1回戦（まだ成績がない）の場合は、出場メンバーをランダムに並べて仮の「順位」とする
   - 過去の他大会での通算成績データがあれば、参考情報として画面に表示する程度に留め、初期シードには使わない（初回起動時や初参加者がいる場合に成立しなくなるため）
2. 第2回戦以降は、前回戦までの大会内累計成績順（3章末尾の集計ルールを参照）を使う。合計点数が同点の場合は選手番号の若い順を優先する。この並び順は、手順3の卓割当・手順4の卓内着席順のいずれにも共通して適用する
3. 卓数 `T` に対して、順位を `T`人ずつのブロックに分割し、ブロックごとに割り当て方向を交互に反転させる（ボウストロフェドン／蛇行配分）
   - 例: T=4（16人）の場合
     - 1〜4位: 卓1=1位, 卓2=2位, 卓3=3位, 卓4=4位（順方向）
     - 5〜8位: 卓4=5位, 卓3=6位, 卓2=7位, 卓1=8位（逆方向）
     - 9〜12位: 卓1=9位, 卓2=10位, 卓3=11位, 卓4=12位（順方向）
     - 13〜16位: 卓4=13位, 卓3=14位, 卓2=15位, 卓1=16位（逆方向）
     - 結果: 卓1={1,8,9,16}, 卓2={2,7,10,15}, 卓3={3,6,11,14}, 卓4={4,5,12,13}（要件の例と一致）
   - Nが4の倍数でない場合は、4.1の抜け番ルールで抜け番を先に決めてから、残りの出場者に対して上記の蛇行配分を行う
4. 各卓内のスタート位置（起家・南家・西家・北家）は、卓組みに使った順位（大会内トータルスコア順、または第1回戦は仮順位）の上位から「東家 → 南家 →西家 → 北家」の順に割り当てる
   - 例: 卓1={1位, 8位, 9位, 16位} の場合、東家=1位, 南家=8位, 西家=9位, 北家=16位
   - 第1回戦はスコアがないため、手順1でランダムに並べた仮順位をそのまま使う
   - このルールは蛇行方式に固有。ワンデー4半荘方式では「各自が東南西北を1回ずつ経験する」ことを優先するため、スコア順の着席は使わない（4.3参照）

### 4.3 ワンデー4半荘方式

**入力**: 参加メンバーリスト（N人）

**制約と優先順位（確定）**:
- 各自が4半荘のうち、実際に出場した回戦の範囲内で東家・南家・西家・北家を1回ずつ経験する（**必達制約・最優先**。蛇行方式のようなスコア順の着席は行わない）
- 4半荘を通して、同じ卓に同席したことのある2人の組み合わせを再度同席させない（**努力目標**。上記の席順一巡と両立しない場合は、席順一巡を優先し、同卓重複を許容する）
- 抜け番は4.1のルールに従い、大会（＝4半荘）を通して全員に均等に回す

**アルゴリズム方針**:
- 人数が16人・20人など、数学的に「4半荘とも全員が別の相手」を厳密に満たす組み合わせが存在する場合でも、事前定義済みの組み合わせ表は持たない（人数パターンごとの保守コストが高いため）
- 汎用的な生成アルゴリズムを採用する:
  1. 半荘1: メンバーをランダムに卓へ振り分け、スタート位置もランダムに割当
  2. 半荘2〜4: 各人の「まだ経験していない位置」を記録しておく。卓分けの際は、各卓に集まる4人の「未経験位置」の組み合わせが東南西北の4席にちょうど割り当てられる（＝席順一巡を成立させられる）組み合わせを優先して探索する
  3. 席順一巡を成立させられる卓分けが複数ある場合は、その中から「これまでに同席した回数」ができるだけ少ないペアが同卓になるものを貪欲法で選ぶ（候補を複数パターン生成し、同席重複数が最小のものを採用する等のランダムリスタート＋局所探索）
  4. 席順一巡を成立させられる卓分けが存在しない場合は、席順一巡の成立を優先し、同卓重複が増えることを許容してでも全員の席順一巡を成立させる卓分けを採用する
- 人数の都合で完全な重複ゼロが不可能な場合（4人・5人・8人・12人など、4半荘分の延べ対戦組数がペア総数を上回ってしまうケース）は、席順一巡が成立していることを前提に、重複数が最小になる近似解を採用する。対応人数を限定したり、エラーで止めたりはしない
- 重複数が同じ卓分け候補が複数生成された場合は、選手番号の小さい人がより若い卓番号に入る候補を優先して一意に決定する（選手番号の昇順に候補同士を比較し、最初に卓番号の差が生じた時点でより小さい卓番号を持つ候補を採用する）

**検証方法**: Phase 0（ロードマップ参照）でユニットテストを書き、代表的な人数（4, 8, 12, 16, 20, 24, 4の倍数でない例として14, 18など）で「スタート位置が出場回戦内で必ず1巡しているか（必達制約）」「その制約の下で同卓重複ペア数ができるだけ少ないか」「抜け番が均等に回っているか」を検証する。

---

## 5. 画面・機能要件

大会・メンバー・回戦それぞれについて、フォーム→保存→一覧→編集・削除のサイクルを実装する（詳細な段階分けは開発ロードマップ.md）。

- ログイン・新規登録（実装済み）: 招待コード制のアカウント新規作成フォーム／ログインフォーム／ログアウト。未ログイン時はメンバー管理画面にアクセスできない
- メンバー管理（実装済み）: 登録フォーム／一覧（登録順・名前順切替）／編集／削除（論理削除＝引退フラグ、確認ダイアログあり、「引退メンバーも表示」で再表示・復帰可）。いずれもログイン中のアカウントにスコープされる
- 大会管理（実装済み・Stage 1）: 作成フォーム（名称・種別・卓組み方式・評価方式・パラメータ・選手番号採番方式・期間）／一覧／編集・削除／参加メンバーの紐付け（既存メンバーからチェックボックスで選択、選手番号を採番）
- 回戦実施（実装済み・Stage 1）: 「次の回戦の卓組みを実行」ボタン→プレビュー表示→保存／回戦一覧／成績入力フォーム（卓ごとに素点4人分まとめて入力、飛び賞チェック）
- 大会内成績表示（実装済み・Stage 1）: 累計順位表（蛇行方式の次回戦入力にも使用）
- ゲスト共有リンク管理（実装済み・Stage 1、主催者側のみ）: 大会詳細画面での発行／再発行（旧リンク失効）／無効化。ゲスト自身が使う閲覧・入力画面はStage 2で対応（3.7参照）
- （将来）大会横断集計（未実装）: メンバーごとの複数大会成績サマリー

---

## 6. 非機能要件・技術方針

- フロントエンド／実行環境: Streamlit
- ホスティング: Railway（Procfileで`streamlit run app.py`を起動。公開URL: https://web-production-7d01b.up.railway.app）
- データ永続化: SQLite（大会・メンバー・回戦・成績という関連データを扱うため、CSV/JSONより構造化クエリに向く）
- 想定利用者: 大会主催者（1〜数名）による運用。認証はstreamlit-authenticator＋bcryptによるアカウント制（招待コードを知る者のみ新規登録可）。データはアカウント単位でスコープされる。アカウント内には管理者（admin）／一般の2ロールがあり、大会管理・回戦実施・ゲスト共有リンクの発行操作は管理者のみ、閲覧は一般ロールでも可能
- 卓組みロジック（`table_logic.py`）・評価方式の集計ロジック（`scoring_logic.py`）は、Streamlit UI・DBから独立した純粋関数として実装済み。pytestで単体テスト可能な形になっている（Phase 0実装済み）
- ゲスト共有リンク（3.7参照）: 大会ごとに発行するトークン付きURLで、アカウント作成・ログインなしに特定の大会1件だけへアクセスできる導線。既存のアカウント制認証（上記）とは別の、限定的な例外的アクセス経路として設計する。トークンはハッシュ化して保存し、主催者がいつでも無効化できる

---

## 7. 点数算出ルール（確定）

大会ごとに評価方式（3.2）を「得点」「ポイント」のいずれかから選ぶ。両方式の集計ロジックはscoring_logic.pyに純粋関数として実装済み（得点ポイント評価方式実装依頼.md対応）。

### 7.1 ①得点評価
- 総合得点 = 素点 ＋ ウマ ＋ オカ（＋飛び賞）
- ウマは「浮き人数（返し点超えの人数、0〜3人）」で参照するテーブルを切り替える。0人浮きの場合はウマ表を使わず、オカのみをトップ（素点1位）に加算する
- 同着の場合、該当順位分の順位点（ウマ・オカ含む）を合計し、同点人数で折半する
- 飛び賞: 半荘中に持ち点が0以下（0またはマイナス）になった選手を「飛んだ人候補」として自動判定し、UI上でチェックボックスの対象にする。ただし飛び賞が実際に発生するかどうかは素点の値そのものではなく、主催者が手入力で指定する「飛ばした人」（1人以上）がチェックされているかどうかだけで決まる。飛んだ選手（チェックが1人以上ある選手）は飛び賞額の全額を負担し、飛ばした人の人数で折半して加算する。素点がちょうど0でも、誰もチェックされなければその選手には減算も加算も発生させない
- 開始点数・返し点・ウマ表・オカ・飛び賞額はすべて大会ごとに主催者が入力する値とし、コードにハードコードしない（`scoring_logic.compute_total_score`の引数）
- 初期値（画面のプレースホルダ）: 35,000点持ち／40,000点返し、1人浮き+48000/-8000/-16000/-24000、2人浮き+24000/+8000/-8000/-24000、3人浮き+12000/+8000/+4000/-24000。オカのデフォルト額は依頼文書に具体的な指定がなかったため、実装では0（未設定）をプレースホルダとしている

### 7.2 ②ポイント評価
- その回戦の素点順位に応じて、大会ごとに入力する順位ポイント表（1〜4位）から順位ポイントを与える（`scoring_logic.compute_rank_points`）。同着は該当順位分を合算して折半する
- 大会を通した累計順位ポイントで順位を決め、同点の場合は素点合計（ウマ・オカを含まない）で二次評価、それも同点なら選手番号の若い順（`ポイント`評価方式はウマ・オカ設定を持たないため。3章末尾参照）
- 初期値（画面のプレースホルダ）: 1位+3 / 2位+1 / 3位-1 / 4位-3

### 7.3 今回のスコープ外
- 卓は4人打ち固定を前提とする（3人打ちは将来拡張の可能性ありだが現時点ではスコープ外）
- 大会横断（複数大会をまたいだ）集計はPhase 6でスコープ外
