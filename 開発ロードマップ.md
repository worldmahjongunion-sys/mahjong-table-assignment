# 麻雀卓組みアプリ — 開発ロードマップ

最終更新: 2026-09-19
前提: 「仕様書.md」の内容に基づく
進捗: Phase 2（メンバー管理）＋ログイン機能＋Railway公開、Phase 0（卓組みロジック本体）、評価方式の集計ロジックに加え、Phase 3〜5相当（大会管理・回戦実施・大会内成績集計）とゲスト共有リンクの発行（Stage 1、PR #28でstagingマージ済み）、**ゲスト自身が使う閲覧・入力画面（Stage 2）も実装完了**（`feature/guest-screens`ブランチ、staging未マージ）。Phase 1（卓組み結果確認用の簡易UI、DB接続なし）はPhase 3〜5がDB接続込みで先行実装されたため実質不要になった可能性が高い。

---

## 優先順位の考え方

依頼では「①フォーム→②保存→③一覧→④編集・削除」の順で作る方針が示されているが、このアプリの本体価値は卓組みロジックの正確さにある。ロジックが崩れていると、その上にどれだけUIを積んでも作り直しになる。そのため、①〜④のサイクルに入る前に「卓組みロジック単体の実装・検証（Phase 0）」を独立させ、最優先で固める。

①〜④のサイクルは、メンバー→大会→回戦の順で、対象ごとに繰り返し適用する。

---

## Phase 0: 卓組みロジックのコア実装（最優先・UIなし）— 実装完了

- 蛇行方式のアルゴリズムをPython関数として実装（`table_logic.py`）
- ワンデー4半荘方式のアルゴリズム（近似解生成含む）をPython関数として実装（`table_logic.py`）
- 抜け番の共通ロジック（均等配分）を実装（`select_absent_members`）
- pytestでユニットテスト（`tests/test_table_logic.py`、`Phase0テストケース.md` 全29ケース相当、green）
  - 代表的な人数パターン（4の倍数／非倍数）での網羅的な検証
  - ワンデー方式: 同席ペアの重複数、スタート位置の1巡、抜け番の均等性を検証
  - 蛇行方式: 指定の配分パターンになることを検証
- 補足（実装時の判断）: ワンデー方式の同卓重複最小化はランダムリスタート＋局所探索によるヒューリスティックのため、人数によっては理論下限に届かない場合がある（仕様書4.3・Phase0テストケース.md B-1で「実装がどこまで0に近づけるかを検証する」とされていた点への回答）。テストでは「理論下限を下回っていないこと」を必須条件とし、下限からの乖離には目安の許容幅を設けている。

## Phase 1: 卓組み結果を確認できる最小UI — 未着手（実質不要の可能性）

- Streamlit上で、メンバー名を直接入力（保存なし・一時的な入力欄）
- 卓組み方式を選択して「卓組みを実行」→結果を画面表示
- DBには接続しない。目的はPhase 0のロジックを実際の画面上で目視確認すること
- Phase 3〜5（大会管理・回戦実施、DB接続あり）が本フェーズより先に実装され、実大会データで卓組みを目視確認できるようになったため、本フェーズを別途実装する必要性は薄い（大会管理とゲスト共有リンク実装依頼.md 3.2にも同旨の記載あり）。着手するかどうかは今後の判断

## Phase 2: メンバー管理（①〜④）— 実装済み

- SQLiteスキーマ作成（メンバーテーブルから開始）
- ①登録フォーム → ②保存 → ③一覧（登録順／名前順） → ④編集・削除
- メンバーは最初に基盤として作る
- 削除は論理削除（is_activeフラグ）。過去の大会・対局記録がメンバーIDに紐づくため物理削除はしない。「引退メンバーも表示」で再表示・復帰が可能。削除操作には確認ダイアログを挟む
- 当初計画になかった追加実装として、ログイン機能（アカウント作成・招待コード制認証）とRailwayへの公開設定も本フェーズで実施済み。メンバーはアカウント（主催者）ごとにスコープされる方式に変更（仕様書2章・3.1参照）

## 評価方式（得点／ポイント）の集計ロジック・表紙画面 — 実装済み

当初計画になかった追加実装として、大会の評価方式（得点評価／ポイント評価、仕様書.md 3.2・7章参照）の集計ロジックと選択UIを先行実装した（得点ポイント評価方式実装依頼.md対応）。

- scoring_logic.py: ①得点評価（ウマ・オカ・飛び賞）②ポイント評価（順位ポイント）の集計ロジックを、table_logic.pyと同じ方針（UI/DB非依存の純粋関数）で実装
- app.py: ログイン後トップに評価方式を選ぶ表紙画面を追加。入力値はこの時点ではst.session_stateにのみ保持していたが、Phase 3で大会作成画面（DB保存）に接続した

## Phase 3: 大会管理（①〜④）— 実装済み（Stage 1）

`大会管理とゲスト共有リンク実装依頼.md`対応、`feature/tournament-management-guest-links`ブランチ（staging未マージ）。

- 大会テーブル（`tournaments`）作成（名称・種別・卓組み方式・評価方式・評価方式パラメータ`scoring_config`・選手番号採番方式・期間・状態。仕様書.md 3.2参照）
- ①作成フォーム → ②保存 → ③一覧 → ④編集・削除
- 大会への参加メンバー紐付け機能（`tournament_members`。既存メンバーからチェックボックスで選択し選手番号を採番）
- 表紙画面で確立した評価方式・パラメータの入力UIをそのまま大会作成・編集フォームに流用してDBへ接続した
- 大会管理・回戦実施は管理者（admin）ロールのみ操作可、一般ロールは閲覧のみ（既存のアカウント内ロール分けを踏襲）

## Phase 4: 回戦の実施・卓組み結果と成績の保存（①〜④）— 実装済み（Stage 1）

- `table_logic.py`のロジックを実データ（大会の参加メンバー・これまでの回戦・成績）に接続する橋渡し層として`tournament_service.py`を新設（DBの保存済みレコード→出場回数・直前抜け番・スタート位置履歴・同卓履歴等の再構築）
- 「次の回戦の卓組みを実行」→プレビュー表示→「保存」で回戦・卓・着席・抜け番のレコードを作成（`rounds`/`tables`/`seats`/`absences`）。2回戦目以降で前回戦までの状態が正しく引き継がれることをテストで確認済み
- 成績入力: 卓ごとに素点4人分をまとめて入力するフォーム（`results`テーブルに素点のみ保存し、総合得点・順位ポイントは表示のたびに`scoring_logic.py`で計算する方針。仕様書.md 3.6参照）
- 飛び賞の「誰が飛ばしたか」入力UI（チェックボックス、仕様書.md 7.3参照）を成績入力フォームに実装（`tobi_busters`テーブル）
- あわせて、ゲスト共有リンク（`tournament_guest_links`）の発行・再発行・無効化を主催者側の大会詳細画面に実装（仕様書.md 3.7参照）。ゲスト自身が使う閲覧・入力画面はStage 2で別途対応（未着手）

## Phase 5: 大会内成績集計 — 実装済み（Stage 1）

- 大会内の累計成績表（大会内順位表）を実装（`tournament_service.compute_standings`）。蛇行方式の次回戦の並び順計算にも使用
- 成績集計ルール（累計得点／累計順位ポイントの合計降順等）自体は仕様書.md 3章末尾・7章・scoring_logic.pyで確定・実装済みだったものを、実データ（大会の評価方式・成績入力）に接続した
- 補足（実装時の判断）: ポイント評価方式の最終順位タイブレーク（累計順位ポイント→累計得点→選手番号）のうち「累計得点」は、ポイント方式の大会がウマ・オカ設定を保持しないため、ウマ・オカ抜きの素点合計で代用している（仕様書.md 3章末尾参照）

## Phase 6（将来）: 大会横断集計

- メンバーIDを軸に、複数大会をまたいだ成績サマリーを表示
- メンバー詳細画面などから参照できるようにする

## ゲスト向け画面（Stage 2）— 実装済み（staging未マージ）

`大会管理とゲスト共有リンク実装依頼.md`が明示的にStage 1のスコープ外としていた部分。Stage 1（PR #28）のstagingマージ後、`ゲスト向け画面実装依頼_Stage2.md`として依頼文章を作成、`feature/guest-screens`ブランチで対応。

- ゲスト共有リンクのトークン付きURL（`?guest=<トークン>`）でアクセスした際の、ログイン・アカウント作成なしの専用画面（`app.py`の`render_guest_view`）。トークンから大会を特定する照合ロジック（Stage 1実装済みの`db.get_tournament_by_guest_token`）をそのまま使う
- 卓組み結果の閲覧（回戦切替、卓ごとの入力済み/未入力表示）、成績（素点）の入力（卓ごとに代表者1人が4人分をまとめて入力）、順位・累計成績の閲覧を実装
- 4章①（実装前確認事項）で判明した「ポイント方式の大会は開始点数を持たない」問題への対応として、ポイント方式の大会にも開始点数だけを追加（ウマ表・オカ・飛び賞額は追加しない）。合計チェック（4人分の素点合計＝開始点数×4）は得点・ポイント両方式で行う。開始点数が未設定の既存大会は成績入力を停止する
- 4章③（実装前確認事項）で判明した「成績レコードに入力日時・入力経路の記録がない」「再発行ボタンの通しテストがない」への対応として、`results`/`tobi_busters`テーブルに`created_at`/`input_source`列を追加（既存行はNULL許容）。ゲストの成績入力は既存の`save_round_results`（削除→挿入で上書き）ではなく、新規`submit_guest_round_results`（新規INSERTのみ）を使い、`results`テーブルの`UNIQUE(round_id, member_id)`制約による`IntegrityError`を`ResultsAlreadySubmittedError`に変換することで、同じ卓への同時送信は先着のみ確定することをDB制約自体で担保する。主催者による修正は従来どおり`save_round_results`の上書きのまま
- 「リンクを再発行する」ボタンを通しで確認するUIテストを追加（旧トークンで大会が特定できなくなり、新トークンで特定できること）

---

## マイルストーンの目安

| Phase | 成果物 | 完了の目安 | 状態 |
|---|---|---|---|
| 0 | 卓組みロジック＋テスト | 全テストパターンが green | 完了 |
| 1 | 卓組み結果確認用の簡易画面 | 実際の人数で目視確認できる | 未着手（Phase 3〜5に代替され実質不要の可能性） |
| 2 | メンバーCRUD＋ログイン＋Railway公開 | メンバーの登録〜削除がDB経由で完結 | 完了 |
| 3 | 大会CRUD | 大会の作成〜削除、参加メンバー紐付けが完結 | 完了（stagingマージ済み・PR #28） |
| 4 | 回戦・成績CRUD | 実大会を1つ最初から最後まで運用できる | 完了（stagingマージ済み・PR #28。ゲスト側画面はStage 2） |
| 5 | 大会内集計 | 蛇行方式が2回戦目以降も正しく動く | 完了（stagingマージ済み・PR #28） |
| 6 | 大会横断集計 | 将来対応（現時点ではスコープ外） | 未着手 |
| ゲスト向け画面（Stage 2） | ゲストがトークンURLで閲覧・入力できる | ログインなしで卓組み結果閲覧・成績入力ができる | 完了（staging未マージ） |
