# 麻雀卓組みアプリ — 開発ロードマップ

最終更新: 2026-08-06
前提: 「仕様書.md」の内容に基づく
進捗: Phase 2（メンバー管理）＋ログイン機能＋Railway公開に加え、**Phase 0（卓組みロジック本体）も実装完了** — 次の最優先タスクはPhase 1（卓組み結果確認用の簡易UI）。

---

## 優先順位の考え方

依頼では「①フォーム→②保存→③一覧→④編集・削除」の順で作る方針が示されているが、このアプリの本体価値は卓組みロジックの正確さにある。ロジックが崩れていると、その上にどれだけUIを積んでも作り直しになる。そのため、①〜④のサイクルに入る前に「卓組みロジック単体の実装・検証（Phase 0）」を独立させ、最優先で固める。

①〜④のサイクルは、メンバー→大会→回戦の順で、対象ごとに繰り返し適用する。

---

## Phase 0: 卓組みロジックのコア実装（最優先・UIなし）— 実装完了

- 蛇行方式のアルゴリズムをPython関数として実装（`table_logic.py`）
- ワンデー4半荘方式のアルゴリズム（近似解生成含む）をPython関数として実装（`table_logic.py`）
- 抜け番の共通ロジック（均等配分）を実装（`select_absent_members`）
- pytestでユニットテスト（`tests/test_table_logic.py`、`Phase0テストケース.md` 全29ケース相当、green）
  - 代表的な人数パターン（4の倍数／非倍数）での網羅的な検証
  - ワンデー方式: 同席ペアの重複数、スタート位置の1巡、抜け番の均等性を検証
  - 蛇行方式: 指定の配分パターンになることを検証
- 補足（実装時の判断）: ワンデー方式の同卓重複最小化はランダムリスタート＋局所探索によるヒューリスティックのため、人数によっては理論下限に届かない場合がある（仕様書4.3・Phase0テストケース.md B-1で「実装がどこまで0に近づけるかを検証する」とされていた点への回答）。テストでは「理論下限を下回っていないこと」を必須条件とし、下限からの乖離には目安の許容幅を設けている。

## Phase 1: 卓組み結果を確認できる最小UI — 未着手

- Streamlit上で、メンバー名を直接入力（保存なし・一時的な入力欄）
- 卓組み方式を選択して「卓組みを実行」→結果を画面表示
- DBには接続しない。目的はPhase 0のロジックを実際の画面上で目視確認すること
- Phase 0（`table_logic.py`）は実装済みのため着手可能。次の最優先タスク

## Phase 2: メンバー管理（①〜④）— 実装済み

- SQLiteスキーマ作成（メンバーテーブルから開始）
- ①登録フォーム → ②保存 → ③一覧（登録順／名前順） → ④編集・削除
- メンバーは最初に基盤として作る
- 削除は論理削除（is_activeフラグ）。過去の大会・対局記録がメンバーIDに紐づくため物理削除はしない。「引退メンバーも表示」で再表示・復帰が可能。削除操作には確認ダイアログを挟む
- 当初計画になかった追加実装として、ログイン機能（アカウント作成・招待コード制認証）とRailwayへの公開設定も本フェーズで実施済み。メンバーはアカウント（主催者）ごとにスコープされる方式に変更（仕様書2章・3.1参照）

## 評価方式（得点／ポイント）の集計ロジック・表紙画面 — 実装済み

当初計画になかった追加実装として、大会の評価方式（得点評価／ポイント評価、仕様書.md 3.2・7章参照）の集計ロジックと選択UIを先行実装した（得点ポイント評価方式実装依頼.md対応）。

- scoring_logic.py: ①得点評価（ウマ・オカ・飛び賞）②ポイント評価（順位ポイント）の集計ロジックを、table_logic.pyと同じ方針（UI/DB非依存の純粋関数）で実装
- app.py: ログイン後トップに評価方式を選ぶ表紙画面を追加。入力値は今回はst.session_stateにのみ保持し、DB保存・大会作成画面への接続はPhase 3・4で行う

## スマホ表示対応 — 確認済み（コード変更なし）

LINEグループでの配布を見据え、既存のログイン後画面をスマホ幅（375〜430px）で確認した（スマホ表示改善依頼.md対応）。候補として挙がっていた4箇所（評価方式ボタン・ウマ表/順位ポイント表の入力欄・会員一覧の並び替え/エクスポート・会員1行の表示）はいずれも、streamlit 1.60の`st.columns`が狭い画面幅で自動的に縦積みへ折り返す挙動により、崩れ・窮屈さ・見切れが発生しないことを確認した。長い氏名・メモでの負荷テスト、横スクロールの有無、サイドバーの開閉動作も合わせて確認し、いずれも問題なし。PC幅（centeredレイアウト）の見た目・挙動は元々変更していないため、そのまま維持されている。

- 確認方法: Chrome DevToolsのモバイルエミュレーション相当（Playwrightでヘッドレスchromiumをスマホ幅のビューポートで起動）で、ログイン→サインアップ→表紙画面→評価方式の各設定フォーム→会員登録→会員一覧の一連の画面を確認
- 未実施: LINE内蔵ブラウザでの実機確認（この開発環境では準備できなかったため、必須ブロッカーとはせず未実施として記録する。実際にLINEでの配布を始める際に、可能なら主催者側で一度確認することを推奨）

## Phase 3: 大会管理（①〜④）

- 大会テーブル作成（名称・種別・卓組み方式・評価方式・期間。評価方式フィールドは仕様書.md 3.2参照）
- ①作成フォーム → ②保存 → ③一覧 → ④編集・削除
- 大会への参加メンバー紐付け機能
- 表紙画面で選んだ評価方式・パラメータ（現在st.session_state保持）をDBへ接続する

## Phase 4: 回戦の実施・卓組み結果と成績の保存（①〜④）

- Phase 0のロジックを実データ（大会の参加メンバー・これまでの成績）に接続
- 「回戦を開始」→卓組み結果（卓割当・起家等）を保存
- 成績（順位・点数）の①入力フォーム → ②保存 → ③一覧 → ④編集・削除。素点入力からscoring_logic.pyで総合得点／順位ポイントを算出する
- 飛び賞の「誰が飛ばしたか」入力UI（チェックボックス、仕様書.md 7.3参照）をここで実装する

## Phase 5: 大会内成績集計

- 大会内の累計成績表を実装（蛇行方式の次回戦の並び順計算に使用）
- 成績集計ルール（累計得点／累計順位ポイントの合計降順等）自体は仕様書.md 3章末尾・7章・scoring_logic.pyで確定・実装済み。本フェーズでは実データ（大会の評価方式・成績入力）への接続を行う

## Phase 6（将来）: 大会横断集計

- メンバーIDを軸に、複数大会をまたいだ成績サマリーを表示
- メンバー詳細画面などから参照できるようにする

---

## マイルストーンの目安

| Phase | 成果物 | 完了の目安 | 状態 |
|---|---|---|---|
| 0 | 卓組みロジック＋テスト | 全テストパターンが green | 完了 |
| 1 | 卓組み結果確認用の簡易画面 | 実際の人数で目視確認できる | 未着手 |
| 2 | メンバーCRUD＋ログイン＋Railway公開 | メンバーの登録〜削除がDB経由で完結 | 完了 |
| 3 | 大会CRUD | 大会の作成〜削除、参加メンバー紐付けが完結 | 未着手 |
| 4 | 回戦・成績CRUD | 実大会を1つ最初から最後まで運用できる | 未着手 |
| 5 | 大会内集計 | 蛇行方式が2回戦目以降も正しく動く | 未着手 |
| 6 | 大会横断集計 | 将来対応（現時点ではスコープ外） | 未着手 |
