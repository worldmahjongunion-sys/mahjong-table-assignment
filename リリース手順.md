# リリース手順

最終更新: 2026-08-26

このアプリを本番（Railway）にリリースする際の標準手順。第8回で実際にこのサイクルを一通り回して確認済み。

---

## 全体の流れ

```
総点検 → 作業ブランチ作成 → コミット・push → PR作成 → main へマージ → Railway 自動デプロイ → 本番URL確認
```

---

## 1. 総点検

作業を始める前に、リポジトリの状態を確認する。

- `git fetch origin --prune` してから `git status` / `git branch -a` でローカルとリモートのズレがないか確認
- `git log --all --oneline --graph` で未マージのブランチが残っていないか確認
- `gh pr list --state all` でオープンなPRがないか確認
- `.gitignore` に `.env` が含まれているか、シークレットが誤ってコミットされていないか確認
- 本番URLが現在正常に応答しているか確認（後述の「本番URL確認」と同じコマンドで事前チェックしておく）

## 2. 作業ブランチ作成

`main` を最新化してから、変更内容がわかるブランチ名で作業ブランチを切る。

```bash
git checkout main
git pull origin main
git checkout -b <種別>/<内容がわかる名前>   # 例: docs/release-runbook, fix/login-error
```

## 3. コミット・push

```bash
git add <変更ファイル>
git commit -m "<変更内容が伝わる一言>"
git push -u origin <ブランチ名>
```

## 4. PR作成

```bash
gh pr create --title "<タイトル>" --body "<変更内容と確認したこと>" --base main
```

## 5. main へマージ

内容を確認し、問題なければマージする。

```bash
gh pr merge <PR番号> --merge   # このリポジトリは通常のマージコミット方式（squashではない）
```

## 6. Railway 自動デプロイ

`main` ブランチへの push をトリガーに Railway が自動でビルド・デプロイする（Railway側でGitHub連携設定済み。リポジトリ内に `railway.json` 等の設定ファイルは無く、Railwayダッシュボード側の設定に依存）。

- デプロイには数十秒〜数分かかる場合がある
- 進捗はRailwayのダッシュボード（Deployments タブ）で確認できる

## 7. 本番URL確認

デプロイ完了後、本番環境が正常に応答することを確認する。

```bash
curl -s -o /dev/null -w "HTTP %{http_code}\n" https://web-production-7d01b.up.railway.app
```

`HTTP 200` が返れば正常。ブラウザでも実際に開き、ログイン画面が表示されることを目視確認する。

---

## 環境

| 環境 | ブランチ | URL |
| --- | --- | --- |
| 本番 (production) | `main` | https://web-production-7d01b.up.railway.app |
| 検証 (staging) | `staging` | Railwayダッシュボードで要確認（未記載） |

---

## 補足

- PRのブランチ名は `edit-a` / `edit-b` のような簡易名も過去に使われているが、以降は種別プレフィックス（`docs/`, `fix/`, `feat/` など）を付けた命名を推奨
- `main` と `staging` の内容に差分がないか、リリース前に `git diff main origin/staging` で確認しておくと安全
