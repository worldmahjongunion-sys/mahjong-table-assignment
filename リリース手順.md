# リリース手順

最終更新: 2026-09-25

このアプリを本番（Railway）にリリースする際の標準手順。第8回で実際にこのサイクルを一通り回して確認済み。

---

## 全体の流れ

```
総点検 → 作業ブランチ作成 → コミット・push → PR作成 → main へマージ → Railway 自動デプロイ → 本番URL確認
```

---

## 1. 総点検

作業を始める前に、リポジトリの状態を確認する。

- `git fetch origin --prune` してから `git status` / `git branch -a` でローカルとリモートのズレがないか確認
- `git log --all --oneline --graph` で未マージのブランチが残っていないか確認
- `gh pr list --state all` でオープンなPRがないか確認
- `.gitignore` に `.env` が含まれているか、シークレットが誤ってコミットされていないか確認
- 本番URLが現在正常に応答しているか確認（後述の「本番URL確認」と同じコマンドで事前チェックしておく）
- **`Procfile`を変更する場合は、Railwayダッシュボードで対象サービス（本番・staging双方）の Settings → Deploy → Custom Start Command を確認する。設定されていて`Procfile`と食い違っている場合、そちらが優先されて`Procfile`の変更が反映されない（詳細・実例は`トラブル復旧ガイド.md`の「4. デプロイは成功しているのにマイグレーションが効かない場合」を参照）**

## 2. 作業ブランチ作成

`main` を最新化してから、変更内容がわかるブランチ名で作業ブランチを切る。

```bash
git checkout main
git pull origin main
git checkout -b <種別>/<内容がわかる名前>   # 例: docs/release-runbook, fix/login-error
```

## 3. コミット・push

```bash
git add <変更ファイル>
git commit -m "<変更内容が伝わる一言>"
git push -u origin <ブランチ名>
```

## 4. PR作成

```bash
gh pr create --title "<タイトル>" --body "<変更内容と確認したこと>" --base main
```

## 5. main へマージ

内容を確認し、問題なければマージする。

```bash
gh pr merge <PR番号> --merge   # このリポジトリは通常のマージコミット方式（squashではない）
```

## 6. Railway 自動デプロイ

`main` ブランチへの push をトリガーに Railway が自動でビルド・デプロイする（Railway側でGitHub連携設定済み。リポジトリ内に `railway.json` 等の設定ファイルは無く、Railwayダッシュボード側の設定に依存）。

- デプロイには数十秒〜数分かかる場合がある
- 進捗はRailwayのダッシュボード（Deployments タブ）で確認できる

### Wait for CI（テスト合格を待ってから反映）

Railwayのサービス設定の「Wait for CI」をオンにすると、pushされたコミットの GitHub Actions（`.github/workflows/tests.yml`）が終わるまでデプロイを待ち、テストが赤ならそのデプロイはスキップされる（前のデプロイが動き続ける）。

| サービス | 状態 |
| --- | --- |
| staging（`web-staging-1395`） | 2026-09-26 にオン。PR #41 のマージで「WAITING FOR CI → テスト合格 → ACTIVE」の一周と `/_stcore/health` の `ok` を確認済み |
| 本番（production） | **まだオフ**。10/2 の実運用が落ち着いてから、staging と同じ手順でオンにする |

- 赤で止まったときに出したい場合は、原因を直して push し直す（Actions の Re-run や Railway の手動 Redeploy もできるが、赤のまま出すことになるので原則しない）
- 赤のときにスキップされる動きは、staging に赤を入れないため実際には試していない

## 7. 本番URL確認

デプロイ完了後、本番環境が正常に応答することを確認する。

```bash
curl -s -o /dev/null -w "HTTP %{http_code}\n" https://web-production-7d01b.up.railway.app
```

`HTTP 200` が返れば正常。ブラウザでも実際に開き、ログイン画面が表示されることを目視確認する。

---

## 環境

| 環境 | ブランチ | URL |
| --- | --- | --- |
| 本番 (production) | `main` | https://app.worldmahjongunion.com（旧URL https://web-production-7d01b.up.railway.app も応答する）。2026-09-21にHobbyプランへアップグレードし復旧済み（経緯は下の「Railway停止と復旧の記録」） |
| 検証 (staging) | `staging` | https://web-staging-1395.up.railway.app（Railway上のサービス `web-staging-1395`）。2026-09-21時点で `/_stcore/health` が `ok` を返すことを確認。ただしRailwayにログインしていないため、どのコミットがデプロイされているかは未確認。Railwayを使わず実機確認したい場合は、`staging`をチェックアウトしローカルで`streamlit run app.py --server.address 0.0.0.0`を起動し、同じWi-Fi内の端末から`http://<PCのローカルIP>:8501`（起動時にターミナルへ表示される"Network URL"）でアクセスする |

---

## Railway停止と復旧の記録（2026-09）

### 何が起きたか

- Railwayの30日お試し（トライアル）期間が切れ、本番が停止した
- **停止期間: 2026-09-06 17:09 〜 2026-09-21 13:52**（UptimeRobotの記録による。以前ここに書いていた「9/19から停止」は誤り）
- 2026-09-21にHobbyプランへアップグレードしたところ、**自動で Online に復帰した（Redeploy操作は不要だった）**

### 復旧後の確認結果（2026-09-21）

- 本番web（app.worldmahjongunion.com）が Online。ACTIVEデプロイは「Merge pull request #24（staging→main）」
- `/_stcore/health` が `ok`、ログインとデータ（web-volume）も無事、UptimeRobotも UP

### 次回以降のために

- 今回は約15日間、監視（UptimeRobot）以外に気づく手段が無かった。プランの期限・残クレジットは、Railwayのダッシュボードで定期的に確認する
- 停止から復帰した直後は、コードを変えていなくても「本番のACTIVEデプロイがどのコミットか」「データ（web-volume）が残っているか」を確認する

---

## API公開前にやること（TRUST_PROXY_HOPS）

`GET /api/tournaments`（`api_server.py`、PR #31）を本番で公開する**前**に、`TRUST_PROXY_HOPS` を実測して設定する。

- 未設定（既定0）のままRailwayのプロキシの後ろに置くと、全員の呼び出し元IPがプロキシのIPになり、認証失敗のレート制限（同一IPから15分に10回失敗で429）が**全員共通**になる。1人の失敗で全員がブロックされる
- 現状、`Procfile` はStreamlitの`$PORT`しか公開しておらず、APIのポートは本番から見えない。したがって`TRUST_PROXY_HOPS`はRailway上で一度も実測されていない
- 実測の手順: `_client_ip()` の中で生の `X-Forwarded-For` を一時的にログ出力 → 正しいトークンで1回呼ぶ → 値の個数と位置を読んで `TRUST_PROXY_HOPS` を決める → ログ出力を消す。**確認用エンドポイントは作らない**
- 設定値の意味: nなら `X-Forwarded-For` の**右からn番目**を呼び出し元IPとする（`api_server.py` の `_client_ip()` 参照）

---

## 本番DBのバックアップと確認（2026-09-25の記録）

PR #39（staging→main 昇格）で実際に使った方法と、想定外だったこと。

### バックアップの取り方

0. 本番Consoleを開いたら、最初に `bash` と手で打って Enter する（下の「想定外だったこと」参照）
1. 本番Consoleで `/data` の中身を確認する（`ls -la /data/`）。`mahjong.db-wal`・`-shm`・`-journal` があるかも見る（2026-09-25 は `mahjong.db` だけだった。このアプリは `journal_mode` を変えていないので、通常 `-wal`/`-shm` はできない）
2. 稼働中のファイルを直接ダウンロードせず、SQLite のバックアップ機能で1ファイルにまとめる（書き込み中でも整合性のとれたコピーになり、`-wal` があればその内容も含まれる）。最後に `('ok',)` と出ればよい

   ```bash
   python -c "import sqlite3; s=sqlite3.connect('/data/mahjong.db'); d=sqlite3.connect('/data/backup_YYYYMMDD.db'); s.backup(d); d.close(); print(sqlite3.connect('/data/backup_YYYYMMDD.db').execute('PRAGMA integrity_check').fetchone())"
   ```

3. `/data/backup_YYYYMMDD.db` をファイルブラウザでダウンロードし、日付入りのファイル名でリポジトリ・OneDrive の外に保管する。`/data` のコピーも復元用に残しておく
4. ダウンロードしたファイルを手元で**読み取り専用**で開き、`integrity_check` と各表の件数を記録する（これを基準値にする）

### 反映後の確認（アプリを開く前、本番Consoleで1行で）

```bash
python -c 'import sqlite3; c=sqlite3.connect("file:/data/mahjong.db?mode=ro",uri=True); print("integrity:",c.execute("PRAGMA integrity_check").fetchone()[0]); T=[r[0] for r in c.execute("SELECT name FROM sqlite_master WHERE type=\"table\" AND name NOT LIKE \"sqlite_%\" ORDER BY name")]; [print("%-26s %6d" % (t, c.execute("SELECT COUNT(*) FROM [%s]" % t).fetchone()[0])) for t in T]'
```

読み取り専用で開くので、DBには書き込まない。既存の表の件数を基準値と比べる（`audit_logs`・`rate_limit_events` はログインすると増える）。

### 想定外だったこと

- **Railway の Backups タブは Pro プラン限定**。Hobby プランでも使えないため、上の「Console＋ファイルブラウザ」で取る
- **Console でコマンドを貼り付けて動かすには、bash に切り替える必要があった**。Console を開いたら、最初に `bash` と手で打って Enter する。これをしないと、貼り付けたコマンドの前後に `^[[200~` と `~` が付いて動かなかった。bash に切り替えた後は、1行のコマンドなら正しく貼り付けられた。また、複数行（ヒアドキュメント）の貼り付けは崩れやすい。Console に貼るコマンドは1行版にする。件数の記録など手元でできることは、ダウンロードしたファイルを手元で開いて行う
- **ファイルブラウザの接続が途中で切れ、ダウンロードが失敗した**。やり直して保存したら、手元で `integrity_check` を実行して、壊れていないファイルかを必ず確かめる（途中で切れたファイルはここで分かる）

### 戻し方（異常時）

1. 先にコードを戻す: 本番 Deployments で前の版のデプロイを Redeploy し、ACTIVE になったことを確かめる（新しいコードのままDBだけ戻すと、再起動時の `init_db()` がまた表を作るため。古いコードは新しい表が残っていても無視する）
2. DBを戻す: Console で `cp /data/backup_YYYYMMDD.db /data/mahjong.db`（`/data` のコピーが無ければファイルブラウザでアップロード）→ Restart
3. 基準値の件数と `integrity_check` を確かめる
4. GitHub の main は、落ち着いてから revert PR で揃える

---

## 補足

- PRのブランチ名は `edit-a` / `edit-b` のような簡易名も過去に使われているが、以降は種別プレフィックス（`docs/`, `fix/`, `feat/` など）を付けた命名を推奨
- `main` と `staging` の内容に差分がないか、リリース前に `git diff main origin/staging` で確認しておくと安全
